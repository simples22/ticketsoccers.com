
/* ===== FILE: about.css ===== */

:root {
--radius-sm:.5rem;
  --radius-xl:1.5rem;
  --radius-full:999px;

   --font-weight-text:300;
  --font-weight-medium:500;
  --font-weight-title:700;
  --font-weight-h1:900;

  --text-xs:.75rem;
  --text-md:1rem;
  --text-lg:1.125rem;
  --text-xl:clamp(1.5rem,8vw,2rem);
  --text-xxl:clamp(1.8rem,8vw,2.5rem);
  --text-hero:clamp(2.8rem,8vw,5.4rem);

  --lh-tight:1.20;
  --lh-normal:1.25;

  --letter-tight:-.06em;
  --letter-normal:0.01em;

}



/* =========================================
   ABOUT HERO SPECIAL
========================================= */

.tslnAboutHeroSpecial{
  position:relative;
  width:100%;
  z-index: 5;
  background: var(--tsln-white);
  padding:2rem var(--container-padding);
}

.tslnAboutHeroSpecialHeader h1{
  font-size: 3rem;
  font-weight: var(--font-weight-h1);
  text-align: center;
}

.tslnAboutHeroSpecialHeader p{
  font-size: 1.3rem;
  color: var(--tsln-muted);
  font-weight: var(--font-weight-medium);
  text-align: center;
}

.tslnAboutHeroMedia{
  position:relative;
  width:min(100%, var(--container-max));
  min-height:23rem;
  margin-inline:auto;
  overflow:hidden;
  box-shadow: var(--shadow-sm);
  border-radius:var(--radius-sm);
  padding-bottom: 3rem;
  background:var(--tsln-blue-dark);
}

.tslnAboutHeroImg,
.tslnAboutHeroVideo{
  width:100%;
  height:100%;
  object-fit:cover;
  object-position:center;
}

.tslnAboutHeroVideo{
  position:absolute;
  inset:0;
}

.tslnAboutHeroOverlay{
  position:absolute;
  inset:0;
  background:linear-gradient(
    90deg,
    rgba(0, 0, 0, 0.452),
    rgba(0,0,0,.20)
  );
}

.tslnAboutHeroContent{
  position:absolute;
  left:calc(var(--container-padding) + 2rem);
  top:50%;
  transform:translateY(-50%);
  z-index:2;
  width:min(100%, 42rem);
  color:var(--tsln-white);
}

.tslnAboutHeroContent h1{
  margin:0;
  font-size:var(--text-hero);
  font-weight:var(--font-weight-h1);
  line-height:1.05;
}


.tslnAboutHeroPlay{
  position:absolute;
  right:2rem;
  bottom:2rem;
  z-index:3;
  min-width:4rem;
  min-height:2.5rem;
  border:1px solid var(--tsln-white);
  border-radius:var(--radius-sm);
  background:rgba(255, 255, 255, 0.14);
  color:var(--tsln-white);
  font-weight:var(--font-weight-title);
  cursor:pointer;
}


/* =========================================
   ABOUT OVERVIEW
========================================= */

.tslnAboutOverviewInner{
  position:relative;
  padding-bottom: 5rem;
}

.tslnAboutOverview{
position:relative;
  width:100%;
  margin-top: 0rem;
  padding:2rem var(--container-padding);
  background: var(--tsln-white);
  z-index: 4;
}

.tslnAboutOverviewInner{
  width:min(100%, var(--container-max));
  margin-inline:auto;
  display:grid;
  grid-template-columns:minmax(20rem, .65fr) minmax(0, 1.35fr);
  gap:8rem;
  background: var(--tsln-white);
  align-items:stretch;
}

.tslnFounderCard{
  order:1;
}

.tslnAboutCompany{
  order:2;
}

/* COMPANY */

.tslnAboutCompany{
  display:flex;
  flex-direction:column;
  background:var(--tsln-white);
  padding: 0;
  padding-bottom: 2rem;
  height:100%;
}

.tslnAboutSection{
  margin-top:2rem;
}


.tslnAboutSection p{
  margin:0;
}


.tslnAboutCompany p{
  margin:0 0 1rem;
  color:var(--tsln-blue-dark);
  line-height:1.8;
}

.tslnAboutCompany p:last-child{
  margin-bottom:0;
}




/* =========================================
   FOUNDER CARD
========================================= */

.tslnFounderCard{
  width:100%;
  max-height:35rem;
  display:flex;
  flex-direction:column;
  overflow:hidden;
  border:1px solid var(--tsln-border);
  border-radius:var(--radius-sm);
  background:var(--tsln-white);
  box-shadow:var(--shadow-sm);
}

.tslnFounderHeader{
  padding:1.5rem 1.5rem 0;
  margin-bottom:1rem;
}

.tslnFounderPhotoWrap{
  width:100%;
  background:transparent;
}

.tslnFounderPhoto{
  position:relative;
  width:100%;
  height:14rem;
  overflow:hidden;
  background:var(--tsln-bg-muted);
}

.tslnFounderImg{
  object-fit:cover;
  object-position:center top;
}

.tslnFounderBody{
  display:flex;
  flex-direction:column;
  gap:.75rem;
  text-align: center;
  padding:1.5rem;
}

.tslnFounderBody h3{
  margin:0;
  color:var(--tsln-blue-dark);
  font-size:var(--text-xl);
  font-weight:var(--font-weight-h1);
  letter-spacing: var(--letter-tight);
  line-height:1.2;
}

.tslnFounderLabel{
  display:inline-flex;
  margin:0;
  color:var(--tsln-blue-dark);
  font-size:var(--text-xs);
  align-items: center;
  text-align: center;
  font-weight:var(--font-weight-title);
  line-height:1.4;
  letter-spacing:.08em;
}

.tslnFounderBody p{
  margin:0;
  display: none;
  color:var(--tsln-muted);
  font-size:var(--text-sm);
}


/* =========================================
   FOUNDER NOTICE
========================================= */

.tslnNoticeFounder{
  width:100%;
  margin-top: 4rem;
  padding: 2rem 0;

}

.tslnNoticeFounder h2{
  margin:0;
  max-width: 20rem;
  border-top: 2px solid var(--tsln-border);
  color:var(--tsln-blue-dark);
  font-size:var(--text-xs);
  font-weight:var(--font-weight-medium);
  line-height:1.4;
}

.tslnNoticeFounder h2 strong{
  display:block;
  margin:.75rem 0;
  font-size:var(--text-xs);
  font-weight:var(--font-weight-title);
}

.tslnNoticeFounder p{
  margin:1rem 0 0;
  color:var(--tsln-text);
  font-size:var(--text-small);
  font-weight:var(--font-weight-legal-medium);
  line-height:1.3;
}




/* =========================================
   ABOUT REGIONS
========================================= */

.tslnAboutRegions{
    position:relative;
    display: none;
  width:100%;
  z-index: 4;
  background: var(--tsln-white);
  padding:3rem var(--container-padding);
  margin-bottom: 2rem;
}

.tslnAboutRegionsInner{
  width:min(100%, var(--container-max));
  margin-inline:auto;
}

.tslnAboutRegionsGrid{
  display:flex;
  align-items:stretch;
  justify-content:space-between;
  gap:1rem;
}

.tslnAboutRegionCard{
  flex:1;
  width: 100%;
  min-height:11rem;
  display:flex;
  flex-direction:column;
  align-items:center;
  justify-content:center;
  gap:1rem;
  padding:0;
  background:var(--tsln-white);
}

.tslnAboutRegionFlag{
  position:relative;
  width:12rem;
  height:6rem;
  overflow:hidden;
  border-radius:var(--radius-xs);
  box-shadow:var(--shadow-sm);
  background:var(--tsln-bg-muted);
}

.tslnAboutRegionImg{
  object-fit:cover;
}

.tslnAboutRegionCard h3{
  margin:0;
  color:var(--tsln-blue-dark);
  font-size:var(--text-md);
  font-weight:var(--font-weight-title);
  text-align:center;
}



/* =========================================
   ABOUT NEXT
========================================= */

.tslnAboutNext {
  position: relative; /* nÃ©cessaire pour que z-index soit actif */
  width: 100%;
  z-index: 1;
  padding: 2rem var(--container-padding);
  background: var(--tsln-blue);
}

.tslnAboutNext:hover {
  background: var(--tsln-blue-dark);
}

.tslnAboutNextInner {
  width: min(100%, var(--container-max));
  margin-inline: auto;
}

.tslnAboutNextCard {
  width: 100%;
  max-width: 60rem;
  margin: 0 auto;
  display: flex;
  align-items: center;
  gap: 1.5rem;
  padding: 1rem;
  border-radius: var(--radius-md);
  background: transparent;
  color: var(--tsln-blue-dark);
  text-decoration: none;
  transition: var(--transition-normal);
}

.tslnAboutNextCard:hover {
  transform: translateY(-1px);
  border-color: var(--tsln-blue);
  box-shadow: var(--shadow-xs);
  background: transparent;
}

.tslnAboutNextMedia {
  position: relative;
  width: 30rem;
  height: 30rem;
  flex-shrink: 0;
  overflow: hidden;
  margin-bottom: 2em;
  border-radius: var(--radius-full);
  background: var(--tsln-blue-dark);
}

.tslnAboutNextMedia:hover {
  background: var(--tsln-bg-muted);
}

.tslnAboutNextImg {
  object-fit: cover;
}

.tslnAboutNextCard h3 {
  margin: 0;
  color: var(--tsln-white);
  text-transform: uppercase;
  font-size: var(--text-xl);
  font-weight: var(--font-weight-title);
  line-height: 1.3;
}



/* =========================================
   ABOUT MISSION CARDS
========================================= */

.aboutMissionCards {
  position: relative;
  z-index: 1;
  width: min(100%, var(--container-max));
  margin-inline: auto;
  padding: 10rem var(--container-padding);
    display: grid;
  grid-template-columns: repeat(2, 1fr);
  align-items: stretch;
  gap: 3rem;
  background: var(--tsln-border);
}

.aboutMissionCard {
  flex: 1;
  padding: 5em;
  border-radius: var(--radius-sm);
  background: var(--tsln-white);
}

.aboutMissionCard p {
  margin: 0;
  color: var(--tsln-blue-dark);
  font-weight: var(--font-weight-medium);
}

/* Featured */
.aboutMissionCardFeatured {
  background: var(--tsln-blue);
}

.aboutMissionCardFeatured p {
  color: var(--tsln-white);
}

.aboutMissionCardFeatured .uiTitle {
  color: var(--tsln-white);
}


/* MOBILE */

@media(max-width:768px){
  .tslnAboutHeroSpecial{
    padding:1rem;
  }

  .tslnAboutHeroSpecialHeader h1{
  font-size: 2.2rem;
  }

  .tslnAboutHeroMedia{
    min-height:16rem;
  }

  .tslnAboutHeroContent{
    left:2rem;
    right:2rem;
    width:auto;
  }

  .tslnAboutHeroPlay{
    right:1rem;
    bottom:1rem;
  }
}

@media (max-width: 768px) {

  .tslnAboutNext {
    padding: 2rem 1rem;
  }

  .tslnAboutNextCard {
    max-width: none;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
  }

  .tslnAboutNextMedia {
    width: 20.5rem;
    height: 20.5rem;
    border-radius: var(--radius-full);
  }
}

/* MOBILE */

@media(max-width:768px){

  .tslnAboutRegions{
    padding:3rem 1rem;
  }

  .tslnAboutRegionsGrid{
    flex-direction:column;
    align-items:stretch;
  }

  .tslnAboutRegionCard{
    min-height:auto;
    flex-direction:row;
    justify-content:flex-start;
    
    padding:0;
  }

  .tslnAboutRegionFlag{
    width:12rem;
    height:6rem;
    flex-shrink:0;
    border-radius: var(--radius-sm);
    box-shadow: var(--shadow-sm);
  }

  .tslnAboutRegionCard h3{
    text-align:left;
  }
}


/* =====================================
   TABLET
========================================= */

@media(max-width:1024px){

  .tslnFounderPhoto{
    height:18rem;
  }

}

/* =====================================
   MOBILE
========================================= */

@media(max-width:768px){

  .tslnAboutOverview{
    padding:3rem 1rem;
  }

  .tslnAboutOverviewInner{
    grid-template-columns:1fr;
    gap:2rem;
  }

  .tslnAboutCompany{
    order:1;
    padding:.10rem;
  }

  .tslnFounderBlock,
  .tslnFounderCard{
    order:2;
  }

  .tslnFounderHeader{
    padding:0;
    margin-bottom:1rem;
  }

  .tslnFounderPhotoWrap{
    padding:0;
  }

  .tslnFounderPhoto{
    height:18rem;
  }

  .tslnFounderBody{
    padding:1.25rem;
    align-items: center;
  }

}


/* =========================================
   TABLET
========================================= */

@media (max-width: 1024px) {
  .aboutMissionCards {
    padding: 3rem var(--container-padding);
    grid-template-columns: 1fr;
  }

  .aboutMissionCard {
    flex: 1 1 calc(50% - .5rem);
  }

  .aboutMissionCards {
    width: 100%;
    padding: 5rem 1rem;
    flex-direction: column; 
    gap: 2rem;
  }

  .aboutMissionCard {
    flex: none;
    padding: 2.5rem;
  }
}







/* =========================================
   UI SECTION FIXED DECOR BACKGROUND
========================================= */

.uiSectionBackgroundBlock {
  position: fixed;
  top: calc(var(--hero-height, 0px) + 0rem); /* adapte --hero-height Ã  ton hero */
  left: 50%;
  transform: translateX(-50%);
  width: 100%;
  height: 50rem;
  z-index: 0;
  border-radius: 0;
  overflow: hidden;
}

/* L'image de fond */
.uiSectionBackgroundBlock::before {
  content: "";
  position: absolute;
  inset: 0;
  background-image:
    linear-gradient(
      to bottom,
      rgba(0, 0, 0, 0.10),
      rgba(0, 0, 0, 0.42)
    ),
    var(--bg-image, none);

  background-size: cover;
  background-position: center;
  background-repeat: no-repeat;
}

.uiSectionBackgroundBlockImg,
.uiSectionBackgroundBlockOverlay {
  display: none;
}


/* Spacer scrollable */
.uiSectionBackgroundBlockSpacer {
  position: relative;
  z-index: 1;
  height: 40rem;
  display: flex;
  align-items: center;
  justify-content: center;
}

.uiSectionBackgroundBlockContent {
  position: flex;
  inset: 0;
  z-index: 3;
  margin: -10rem 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 1rem;
  padding: 0rem var(--container-padding);
  text-align: center;
}

.uiSectionBackgroundBlockTitle {
  margin: 0;
  color: var(--tsln-white);
  font-size: var(--text-hero);
  font-weight: var(--font-weight-h1);
  line-height: var(--lh-tight);
  letter-spacing: var(--letter-tight);
  max-width: 45rem;
}

.uiSectionBackgroundBlockText {
  margin: 0;
  color: var(--tsln-white);
  font-size: var(--text-md);
  font-weight: var(--font-weight-medium);
  max-width: 45rem;
}


.uiSectionBackgroundBlockSpacer {
  height: 40rem;
}

@media (max-width: 768px) {
  .uiSectionBackgroundBlockSpacer {
    height: 40rem;
  }
}

@media (max-width: 768px) {
  .uiSectionBackgroundBlock {
    width: 100%;
    height: 40rem;
  }
}

/* ===== FILE: categories.css ===== */

/* =========================================
   CATEGORY RAIL (CategorySection.tsx)
========================================= */
.tslnCategoryRail{
  width:min(100%, var(--container-max));
  margin-inline:auto;
  padding:2rem var(--container-padding);
}
.tslnCategoryHead{
  display:flex;
  align-items:flex-end;
  justify-content:space-between;
  gap:1rem;
  flex-wrap:wrap;
  margin-bottom:1.25rem;
}
.tslnCategoryHead .tslnSectionHeader{ flex:1; min-width:0; max-width:46rem; }
.tslnCategoryHead .tslnSectionHeader p{
  margin:.4rem 0 0;
  color:var(--tsln-muted);
  line-height:var(--lh-normal);
}
.tslnCategoryControls{ display:flex; gap:.5rem; flex-shrink:0; }
.tslnCategoryScrollBtn{
  width:2.75rem;
  height:2.75rem;
  display:flex;
  align-items:center;
  justify-content:center;
  border:1px solid var(--tsln-border);
  border-radius:var(--radius-full);
  background:var(--tsln-white);
  color:var(--tsln-blue);
  cursor:pointer;
  transition:var(--transition-fast);
}
.tslnCategoryScrollBtn:hover{ background:var(--tsln-blue); color:var(--tsln-white); border-color:transparent; }

.tslnCategoryTrack{
  width:100%;
  display:flex;
  gap:1rem;
  overflow-x:auto;
  overflow-y:hidden;
  padding:.25rem 0 .75rem;
  scroll-snap-type:x mandatory;
  scrollbar-width:none;
  -ms-overflow-style:none;
}
.tslnCategoryTrack::-webkit-scrollbar{ display:none; }

.tslnCategoryCard{
  flex:0 0 18rem;
  scroll-snap-align:start;
}
.tslnCategoryLink{
  position:relative;
  display:block;
  width:100%;
  height:14rem;
  overflow:hidden;
  border:0;
  padding:0;
  border-radius:var(--radius-sm);
  background:var(--tsln-blue-dark);
  color:var(--tsln-white);
  text-align:left;
  cursor:pointer;
  box-shadow:var(--shadow-sm);
}
.tslnCategoryMedia{ position:absolute; inset:0; }
.tslnCategoryImg{ object-fit:cover; object-position:center; transition:transform .35s ease; }
.tslnCategoryLink:hover .tslnCategoryImg{ transform:scale(1.04); }
.tslnCategoryOverlay{
  position:absolute;
  inset:0;
  background:linear-gradient(180deg, rgba(0,0,0,.05), rgba(0,0,0,.55));
}
.tslnCategoryContent{
  position:absolute;
  left:1.25rem;
  right:1.25rem;
  bottom:1.25rem;
  z-index:2;
}
.tslnCategoryLabel{
  margin:0;
  color:var(--tsln-white);
  font-size:var(--text-xl);
  font-weight:var(--font-weight-h1);
  line-height:1.1;
}
.tslnCategoryText{
  margin:.3rem 0 0;
  color:rgba(255,255,255,.85);
  font-size:var(--text-xs);
  font-weight:var(--font-weight-medium);
}

/* category search reveal (when a category is selected) */
.tslnCategorySearch{ width:100%; margin-top:1.25rem; }
.tslnCategorySearchDesktop{ display:block; }
.tslnCategorySearchMobile{ display:none; }
.tslnCategorySearchBtn{ margin-inline:auto; }

@media(max-width:768px){
  .tslnCategoryRail{ padding:1.5rem 1rem; }
  .tslnCategoryHead{ align-items:flex-start; }
  .tslnCategoryControls{ display:none; }
  .tslnCategoryCard{ flex:0 0 80%; }
  .tslnCategoryLink{ height:11rem; }
  .tslnCategorySearchDesktop{ display:none; }
  .tslnCategorySearchMobile{ display:block; }
}


/* ===== FILE: contact.css ===== */

:root{
  --tsln-panel-soft:#afafaf3f;
}

/* =========================================
   CONTACT SUPPORT GRID
========================================= */

.tslnContactSupport{
  width:100%;
  padding: 2rem 0;
}

.tslnContactSupport{
  background:#afafaf3f;
}

.tslnContactSupportInner{
  width:100%;
  padding:2rem;

  background:
    radial-gradient(
      circle at top left,
      rgba(0, 69, 197, 0.616),
      transparent 34%
    ),
    var(--tsln-bg-soft);
  box-shadow:var(--shadow-xs);
}

.tslnContactSupportHeader{
  max-width:48rem;
}

.tslnContactSupportHeader p{
  margin:1rem 0 0;
  color:var(--tsln-muted);
  line-height:1.7;
}

/* GRID */

.tslnContactSupportGrid{
  width:100%;
  margin-top:1.5rem;
  display:grid;
  grid-template-columns:repeat(2, minmax(0, 1fr));
  gap:1rem;
  align-items:start;
}

.tslnContactSupportItem{
  width:100%;
  position:relative;
}

/* CARD BUTTONS */

.tslnContactSupportCard{
  width:100%;
  min-height:5.5rem;

  display:flex;
  align-items:center;
  justify-content:space-between;
  gap:1rem;

  padding:1rem 1.25rem;
  border:1px solid var(--tsln-border);
  border-radius:var(--radius-sm);
  background:var(--tsln-white);
  color:var(--tsln-blue-dark);
  font:inherit;
  font-weight:var(--font-weight-title);
  text-decoration:none;
  text-align:left;
  cursor:pointer;
  box-shadow:var(--shadow-sm);
  transition:
    background .25s ease,
    border-color .25s ease,
    box-shadow .25s ease,
    transform .25s ease;
}

.tslnContactSupportCard:hover{
  border-color:var(--tsln-blue);
  background:var(--tsln-blue-soft);
  box-shadow:var(--shadow-md);
  transform:translateY(-1px);
}

.tslnContactSupportText{
  color:var(--tsln-blue-dark);
  font-size:var(--text-md);
  font-weight:var(--font-weight-title);
  line-height:1.45;
}

.tslnContactSupportIcon{
  width:2rem;
  height:2rem;

  display:inline-flex;
  align-items:center;
  justify-content:center;
  flex-shrink:0;
  color:var(--tsln-blue);
  font-size:var(--text-md);
  transform:rotate(-90deg);
  transition:
    transform .25s ease,
    color .25s ease;
}

.tslnContactSupportCard:hover .tslnContactSupportIcon{
  color:var(--tsln-blue-dark);
}

.tslnContactSupportItem.open .tslnContactSupportIcon{
  transform:rotate(0deg);
  color:var(--tsln-blue-dark);
}


/* REGION OVERLAY PANEL */

.tslnContactRegionPanel{
  position:absolute;
  top:calc(100% + -.5rem);
  left:0;
  right:0;
  z-index:90;
  width:100%;
  padding:1rem;
  display:grid;
  gap:.15rem;
  border:1px solid var(--tsln-border);
  border-radius:0;
  background:var(--tsln-border);
  box-shadow:var(--shadow-md);
}

.tslnContactRegionLink{
  display:flex;
  flex-direction:column;
  gap:.2rem;
  padding:.85rem;
  border-radius:var(--radius-lg);
  color:var(--tsln-blue-dark);
  transition:
    background .25s ease,
    color .25s ease;
}

.tslnContactRegionLink:hover{
  background:var(--tsln-blue-soft);
  color:var(--tsln-blue);
  
  text-decoration: underline;
}

.tslnContactRegionLink span{
  font-weight:var(--font-weight-title);
}

.tslnContactRegionLink small{
  color:var(--tsln-muted);
  font-size:var(--text-sm);
}





/* =========================================
   CONTACT PAGE
========================================= */

.tslnContactPage{
  width:100%;
  background: var(--tsln-panel-soft);
}

.tslnContactContainer{
  width:min(100%, var(--container-max));
  margin-inline:auto;
  padding:4rem var(--container-padding);
}

.tslnContactHeader{
  max-width:100%;
  margin:0 auto 2rem;
  text-align:flex-start;
}


.tslnContactHeader p{
  margin:0;
  color:var(--tsln-blue-dark);
  line-height:var(--lh-normal);
  font-size:var(--text-md);
}

/* =========================================
   CONTACT FORM
========================================= */

.tslnContactForm{
  width:min(100%, 60rem);
  margin-inline:auto;
  display:grid;
  gap:1rem;
  padding:2rem;
  border:1px solid var(--tsln-border);
  border-radius:var(--radius-sm);
  background:var(--tsln-white);
  box-shadow:var(--shadow-sm);
}

.tslnHoneyField{
  position:absolute;
  left:-9999px;
  width:1px;
  height:1px;
  overflow:hidden;
}

.tslnContactGrid{
  display:grid;
  grid-template-columns:repeat(2,minmax(0,1fr));
  gap:1rem;
}

.tslnContactForm input,
.tslnContactForm textarea{
  width:100%;
  padding:1rem;
  border:1px solid var(--tsln-border);
  border-radius:var(--radius-sm);
  background:var(--tsln-bg);
  color:var(--tsln-text);
  font:inherit;
  transition:var(--transition-fast);
}

.tslnContactForm input:focus,
.tslnContactForm textarea:focus{
  outline:none;
  border-color:var(--tsln-blue);
}

.tslnContactForm textarea{
  min-height:12rem;
  resize:vertical;
}

.tslnContactForm .tslnBtn{
  border:none;
  cursor:pointer;
}

.tslnContactNotice{
  margin:0;
  font-weight:var(--font-weight-medium);
}

.tslnContactNotice.success{
  color:#16a34a;
}

.tslnContactNotice.error{
  color:#dc2626;
}

/* =========================================
   MOBILE
========================================= */


@media(max-width:768px){


  .tslnContactSupportInner{
    padding:1rem;
    border-radius:var(--radius-md);
  }

  .tslnContactSupportGrid{
    grid-template-columns:1fr;
  }

  .tslnContactSupportCard{
    min-height:4.75rem;
  }

  .tslnContactRegionPanel{
    position:absolute;
    top:calc(100% + -.5rem);
    z-index:50;
  }

}


@media(max-width:768px){

  .tslnContactContainer{
    padding:2rem 1rem;
  }

  .tslnContactHeader{
    margin:2rem 0;
  }

  .tslnContactHeader h1{
    font-size:var(--text-lg);
  }

  .tslnContactForm{
    padding:1rem;
  }

  .tslnContactGrid{
    grid-template-columns:1fr;
  }

  .tslnContactForm .tslnBtn{
    width:100%;
  }

}

/* ===== FILE: cookiesnotice.css ===== */

/* =========================================
   COOKIES / AFFILIATE NOTICE
========================================= */

.tslnCookiesNotice{
  position:fixed;
  left:1rem;
  right:1rem;
  bottom:.55rem;
  z-index:99998;
  display:flex;
  justify-content:center;
  pointer-events:none;
}

.tslnCookiesNoticeCard{
  position:relative;
  width:min(100%, 0);
  padding:1.25rem 3.25rem 1.25rem 1.25rem;
  border:1px solid var(--tsln-border);
  border-radius:var(--radius-sm);
  background:var(--tsln-white);
  color:var(--tsln-white);
  box-shadow:var(--shadow-md);
  pointer-events:auto;
  animation:tslnCookiesNoticeIn .35s ease both;
}

.tslnCookiesNoticeClose{
  position:absolute;
  top:.75rem;
  right:.75rem;
  width:2rem;
  height:2rem;
  display:flex;
  align-items:center;
  justify-content:center;
  border:1px solid var(--tsln-blue);
  border-radius:var(--radius-sm);
  background:var(--tsln-bg);
  color:var(--tsln-blue);
  font-size:1.2rem;
  cursor:pointer;
}

.tslnCookiesNoticeLabel{
  display:inline-flex;
  margin-bottom:.6rem;
  color:var(--tsln-blue-dark);
  font-size:var(--text-xs);
  font-weight:var(--font-weight-title);
  text-transform:uppercase;
  letter-spacing:.08em;
}

.tslnCookiesNoticeCard h2{
  margin:0 0 .65rem;
  color:var(--tsln-blue-dark);
  font-size:var(--text-xs);
  font-weight:var(--font-weight-title);
}

.tslnCookiesNoticeCard p{
  margin:0;
  color:var(--tsln-blue-dark);
  font-size:var(--text-small);
  font-weight: var(--font-weight-legal-medium);
  line-height:1.18;
}

.tslnCookiesNoticeSmall{
  margin-top:.75rem !important;
  font-size:var(--text-xs) !important;
}

@keyframes tslnCookiesNoticeIn{
  from{
    opacity:0;
    transform:translateY(1rem);
  }

  to{
    opacity:1;
    transform:translateY(0);
  }
}









/* =========================================
   SCROLL TOP BUTTON
========================================= */

.tslnScrollTopBtn{
  position:fixed;
  right:1.5rem;
  bottom:1.5rem;
  z-index:90;
  min-width:3.25rem;
  height:3.25rem;
  display:flex;
  align-items:center;
  justify-content:center;
  gap:.4rem;
  padding:0 1rem;
  border:1px solid var(--tsln-blue);
  border-radius:var(--radius-sm);
  background:var(--tsln-blue);
  color:var(--tsln-white);
  box-shadow:var(--shadow-md);
  cursor:pointer;
  opacity:0;
  visibility:hidden;
  transform:
    translateY(1rem)
    scale(.95);
  transition:
    opacity .3s ease,
    visibility .3s ease,
    transform .3s ease;
}

.tslnScrollTopBtn.visible{
  opacity:1;
  visibility:visible;
  transform:
    translateY(0)
    scale(1);
}

.tslnScrollTopIcon{
  font-size:2rem;
}

.tslnScrollTopBtn:hover .tslnScrollTopIcon{
  transform:translateY(-2px);
}

.tslnScrollTopBtn:hover{
  background:var(--tsln-blue-dark);
}

.tslnScrollTopBtn span{
  font-size:var(--text-sm);
  font-weight:var(--font-weight-title);
}




/* =========================================
   TOP NOTICE BAR
========================================= */

.tslnTopNoticeBar{
  width:100%;
  min-height:2rem;
  display: none !important;
  z-index: 4;
  top: 0;
  display:flex;
  align-items:left;
  justify-content:center;
  background: var(--tsln-blue);
}

.tslnTopNoticeInner{
  width:min(100%, var(--container-max));
  display:flex;
  align-items:center;
  justify-content:right;
  padding:.45rem var(--container-padding);
}

.tslnTopNoticeText{
  color:var(--tsln-white);
  font-size:var(--text-xs);
  font-weight:var(--font-weight-titmediumle);
  font-style: italic;
  text-align:right;
  letter-spacing:.03em;
}

/* MOBILE */
@media(max-width:768px){

  .tslnTopNoticeBar{
    min-height:1.8rem;
    background: var(--tsln-white-cookies);
  }

  .tslnTopNoticeInner{
    padding:1rem 1rem;
  }

  .tslnTopNoticeText{
    font-size:var(--text-small);
    line-height:1.3;
  }
}

/* MOBILE */

@media(max-width:768px){

  .tslnScrollTopBtn{
    right:1rem;
    bottom:1rem;
    min-width:3rem;
    height:3rem;
    padding:0 .85rem;
  }

  .tslnScrollTopBtn span{
    display:none;
  }

  .tslnScrollTopIcon{
  font-size:2rem;
}
} 

@media(max-width:768px){
  .tslnCookiesNotice{
    left:0;
    right:0;
    bottom:0rem;
  }

  .tslnCookiesNoticeCard{
    padding:1rem 3rem 1rem 1rem;
  }
}

/* ===== FILE: events.css ===== */



/* =========================================
   UI CAROUSEL UNIVERSAL
========================================= */

.uiCarousel{
  width:100%;
  padding:3rem var(--container-padding);
}

.uiCarouselHead{
  width:min(100%, var(--container-max));
  margin:0 auto 1rem;
}

.uiCarouselHead h2{
  margin:0;
  color:var(--tsln-blue-dark);
  font-size:var(--text-xl);
  font-weight:var(--font-weight-h1);
}

.uiCarouselHead p{
  margin:.5rem 0 0;
  color:var(--tsln-muted);
  line-height:1.6;
}

.uiCarouselTrack{
  width:min(100%, var(--container-max));
  margin-inline:auto;
  display:flex;
  gap:1rem;
  overflow-x:auto;
  overflow-y:hidden;
  padding:.25rem 0 1rem;
  scrollbar-width:none;
  -ms-overflow-style:none;
  scroll-snap-type:x proximity;
  -webkit-overflow-scrolling:touch;
}

.uiCarouselTrack::-webkit-scrollbar{
  display:none;
}

.uiCarouselTrack > *{
  flex:0 0 auto;
  scroll-snap-align:start;
}

.uiCarouselSkeletonCard{
  width:18rem;
  min-height:16rem;
  flex:0 0 18rem;
}

@media(max-width:768px){
  .uiCarousel{
    padding:2.5rem 1rem;
  }

  .uiCarouselSkeletonCard{
    width:16rem;
    flex-basis:16rem;
  }
}

.tslnBadge{
  display:inline-flex;
  align-items:center;
  min-height:1.75rem;
  padding:0 .65rem;
  border-radius:999px;
  background:var(--tsln-bg-muted);
  color:var(--tsln-blue-dark);
  font-size:.75rem;
  font-weight:700;
}

.tslnBadge--hot,
.tslnBadge--featured{
  background:var(--tsln-blue);
  color:var(--tsln-white);
}

.tslnBadge--soldout{
  background:#111827;
  color:#fff;
}

.tslnPriceTag{
  display:inline-grid;
  gap:.15rem;
}

.tslnPriceTag small{
  color:var(--tsln-muted);
  font-size:.75rem;
}

.tslnPriceTag strong{
  color:var(--tsln-blue-dark);
}

.tslnTopUpLoader{
  display:inline-flex;
  gap:.35rem;
}

.tslnTopUpLoader span{
  width:.5rem;
  height:.5rem;
  border-radius:999px;
  background:var(--tsln-blue);
}

.tslnEmptyState{
  padding:2rem;
  border:1px solid var(--tsln-border);
  border-radius:var(--radius-sm);
  background:var(--tsln-white);
  text-align:center;
}


/* ===== FILE: faqs.css ===== */


:root{
  --tsln-blue:#0045c5;
  --tsln-blue-hover:#003699;
  --tsln-blue-soft:rgba(0,89,255,.10);
  --tsln-blue-dark:#000000;

  --tsln-bg:#ffffff;
  --tsln-bg-muted:#f1f5f9;
  --tsln-panel-soft:rgba(255,255,255,.86);

  --tsln-text:#111827;
  --tsln-muted:#6b7280;
  --tsln-white:#ffffff;
  --tsln-panel-soft:#afafaf3f;
    --tsln-white-muted: #bdbdbd59;   
    --tsln-white-soft:  #001b747c;   
    --tsln-white-light: #ffffff14;   
    --tsln-white-glass: #ffffff1a;   
    --tsln-white-hover: #ffffff86;   

  --text-sm:1.012rem;
  --text-xs:.90rem;

  --tsln-border:#e5e7eb;
}




/* =========================================
   HELP HERO PAGE
========================================= */

.tslnHelpHeroPage{
  position:relative;
  width:100%;
  min-height:20rem;
  overflow:hidden;
  background:var(--tsln-blue-dark);
  color:var(--tsln-white);
}

.tslnHelpHeroBg{
  position:absolute;
  inset:0;
  z-index:0;
}

.tslnHelpHeroImg{
  object-fit:cover;
  object-position:center;
}

.tslnHelpHeroOverlay{
  position:absolute;
  inset:0;
  z-index:1;
  background:
    linear-gradient(
      180deg,
      rgba(0,0,0,.80),
      rgba(0,0,0,.68)
    );
}

.tslnHelpHeroInner{
  position:relative;
  z-index:2;
  width:min(100%, var(--container-max));
  min-height:20rem;
  margin-inline:auto;
  padding:4rem var(--container-padding);
  display:grid;
  grid-template-columns:minmax(0,1.2fr) minmax(18rem,.8fr);
  gap:clamp(2rem, 8vw, 10rem);
  align-items:center;
}

.tslnHelpHeroLeft{
  text-align:left;
}

.tslnHelpHeroLeft span{
  display:inline-flex;
  margin-bottom:1rem;
  color:rgba(255,255,255,.78);
  font-size:var(--text-xs);
  font-weight:var(--font-weight-title);
  text-transform:uppercase;
  letter-spacing:.08em;
}

.tslnHelpHeroLeft h1{
  margin:0;
  color:var(--tsln-white);
  font-size:clamp(3rem,5vw,5rem);
  font-weight:var(--font-weight-h1);
  line-height:1.05;
}

.tslnHelpHeroRight{
  justify-self:end;
  width:100%;
  max-width:28rem;
  margin-top:5rem;
  padding-top:2rem;
  border-top:2px solid var(--tsln-white);
  text-align:right;
}

.tslnHelpHeroRight h2{
  margin:0;
  color:var(--tsln-white);
  font-size:var(--text-lg);
  font-weight:var(--font-weight-h1);
  line-height:1.05;
}

.tslnHelpHeroLeft p,
.tslnHelpHeroRight p{
  margin:1rem 0 0;
  color:var(--tsln-bg-muted);
  font-size:var(--text-lg);
  font-weight:var(--font-weight-medium);
  line-height:1.7;
}

.tslnHelpHeroRight p{
  margin-left:auto;
  font-size: var(--text-small);
  max-width:28rem;
}





/* =========================================
   FAQ HELP GUIDE
========================================= */

.tslnFaqHelp{
  width:100%;
  padding:4rem var(--container-padding);
  margin-bottom: 0;
  background: var(--tsln-white-muted);
}

.tslnFaqHelpInner{
  width:min(100%, var(--container-max));
  margin-inline:auto;
  display:grid;
  grid-template-columns:minmax(0,1fr) 30rem;
  gap:2rem;
  align-items:start;
}

/* =========================================
   ACCORDION
========================================= */

.tslnFaqHelpAccordionBtn{
  width:100%;
  display:flex;
  align-items:center;
  justify-content:space-between;
  gap:1rem;
  padding:.55rem 1.25rem;
  border:1px solid var(--tsln-bg-muted);
  border-radius:var(--radius-sm);
  background:var(--tsln-white);
  box-shadow:var(--shadow-xs);
  cursor:pointer;
 
  text-align:left;
  transition:var(--transition-fast);
}

.tslnFaqHelpAccordionBtn:hover{
  border-color:var(--tsln-border);
  box-shadow:var(--shadow-md);
}

.tslnFaqHelpContent h1{
  font-size: var(--text-md);
}

.tslnFaqHelpAccordionPanel{
  padding-top:1.5rem;
}

.tslnFaqHelpAccordionIcon{
  flex-shrink:0;
  color:var(--tsln-blue);
  font-size:1rem;
  transition:
    transform .25s ease,
    color .25s ease;
}

.tslnFaqHelpAccordionIcon.open{
  transform:rotate(-180deg);
}

.tslnFaqHelpAccordionBtn:hover .tslnFaqHelpAccordionIcon{
  color:var(--tsln-blue-dark);
}

/* =========================================
   CONTENT
========================================= */

.tslnFaqHelpContent{
  display:grid;
  gap:3rem;
}

.tslnFaqHelpIntro p,
.tslnFaqHelpStep p{
  margin:0;
  color:var(--tsln-blue-dark);
  font-weight:var(--font-weight-medium);
  line-height:1.8;
}

.tslnFaqHelpStep{
  padding:2rem 0;
}

/* =========================================
   MAIN IMAGE
========================================= */

.tslnFaqHelpImage{
  position:relative;
  width:100%;
  height:22rem;
  margin-top:1.25rem;
  padding:.5rem;
  overflow:hidden;
  border:1px solid var(--tsln-border);
  border-radius:var(--radius-sm);
  background:var(--tsln-white);
  box-shadow:var(--shadow-sm);
}

.tslnFaqHelpImg{
  object-fit:cover;
  border-radius:var(--radius-sm);
}

/* =========================================
   NOTICE
========================================= */

.tslnFaqHelpNotice{
  padding:1.25rem;
  border-left:2px solid var(--tsln-blue-dark);
  background:var(--tsln-bg-muted);
}

.tslnFaqHelpNotice strong{
  display:block;
  margin-bottom:.5rem;
  color:var(--tsln-blue-dark);
  font-weight:var(--font-weight-title);
}

.tslnFaqHelpNotice p{
  margin:0;
  color:var(--tsln-blue-dark);
  font-size:var(--text-xs);
}


/* =========================================
   ACTIONS
========================================= */

.tslnFaqHelpActions{
  display:flex;
  align-items:center;
  flex-wrap:wrap;
  gap:1rem;
  margin-top:1rem;
}

.tslnFaqHelpActions a{
  display:inline-flex;
  align-items:center;
  color:var(--tsln-blue);
  font-size:var(--text-xs);
  font-weight:var(--font-weight-title);
  text-decoration:none;
  transition:var(--transition-fast);
}

.tslnFaqHelpActions a:hover{
  color:var(--tsln-blue-dark);
}

/* =========================================
   SIDEBAR
========================================= */

.tslnFaqHelpAd{
  position:sticky;
  top:6rem;
}

.tslnFaqHelpCards{
  display:flex;
  flex-direction:column;
  gap:1rem;
}

/* =========================================
   CARDS
========================================= */

.tslnFaqHelpCard{
  overflow:hidden;
  padding:.35rem;
  border:1px solid var(--tsln-border);
  border-radius:var(--radius-sm);
  background:var(--tsln-white);
  box-shadow:var(--shadow-sm);
  transition:
    transform .25s ease,
    box-shadow .25s ease,
    border-color .25s ease;
}

.tslnFaqHelpCard:hover{
  transform:translateY(-2px);
  border-color:var(--tsln-blue);
  box-shadow:var(--shadow-md);
}

.tslnFaqHelpAdMedia{
  position:relative;
  width:100%;
  height:14rem;
  overflow:hidden;
  border-radius:var(--radius-sm);
  background:var(--tsln-bg-muted);
}

.tslnFaqHelpAdImg{
  object-fit:cover;
  transition:transform .4s ease;
}

.tslnFaqHelpCard:hover .tslnFaqHelpAdImg{
  transform:scale(1.05);
}

.tslnFaqHelpAdBody{
  padding:1rem;
}

.tslnFaqHelpAdBody span{
  display:inline-flex;
  margin-bottom:.5rem;
  color:var(--tsln-muted);
  font-size:var(--text-xs);
  font-weight:var(--font-weight-title);
}

.tslnFaqHelpAdBody h3{
  margin:0 0 .5rem;
  color:var(--tsln-blue-dark);
  font-size:var(--text-md);
  font-weight:var(--font-weight-title);
}

.tslnFaqHelpAdBody p{
  margin:0;
  color:var(--tsln-text);
  font-size:var(--text-small);
  font-weight: var(--font-weight-medium);
  line-height:var(--lh-normal);
}






/* =========================================
   FAQS
========================================= */

.tslnFaqSection,
.tslnFaqPage{
  width:100%;
  padding:4rem var(--container-padding);
  background: #afafaf3f;
}

.tslnFaqInner{
  width:min(100%, var(--container-max));
  margin-inline:auto;
}

/* =========================================
   FAQ DEFAULT LIST
========================================= */

.tslnFaqList{
  display:grid;
  grid-template-columns:1fr;
  gap:.85rem;
  margin-top:1rem;
}

/* =========================================
   FAQ 2 COLUMNS
========================================= */

.tslnFaqColumns{
  width:100%;
  display:grid;
  grid-template-columns:repeat(2, minmax(0, 1fr));
  align-items:start;
  gap:1rem;
  margin-top:1rem;
}

.tslnFaqColumn{
  min-width:0;
  display:flex;
  flex-direction:column;
  gap:.85rem;
}

/* =========================================
   FAQ ITEM
========================================= */

.tslnFaqItem{
    position: relative;
  width:100%;
  overflow:visible;
  border-radius:var(--radius-xs);
  background:var(--tsln-white);
  box-shadow:var(--shadow-sm);
  transition:var(--transition-normal);
}

.tslnFaqItem.open{
  box-shadow:var(--shadow-md);
}

.tslnFaqItem.open .tslnFaqQuestion{
  background:var(--tsln-blue-soft);
}

/* =========================================
   FAQ QUESTION
========================================= */

.tslnFaqQuestion{
  position:relative;

  width:100%;
  min-height:2.5rem;

  display:flex;
  align-items:center;
  justify-content:space-between;
  gap:1rem;
  padding:1rem;
  border:1px solid var(--tsln-border);
  border-radius:var(--radius-xs);
  background:var(--tsln-white);
  color:var(--tsln-blue-dark);
  font:inherit;
  font-weight:var(--font-weight-title);
  text-align:left;
  cursor:pointer;
  overflow:hidden;
  transition:var(--transition-fast);
}

.tslnFaqQuestion::after{
  content:"";
  position:absolute;
  left:0;
  right:0;
  bottom:0;
  height:2px;
  background:var(--tsln-blue);
  transform:scaleX(0);
  transform-origin:left center;
  transition:transform .35s ease;
}

.tslnFaqItem:hover .tslnFaqQuestion::after,
.tslnFaqItem.open .tslnFaqAnswer::after{
  transform:scaleX(1);
}

.tslnFaqItem.open .tslnFaqQuestion::after{
  transform:scaleX(0);
}

.tslnFaqQuestionText{
  position:relative;
  z-index:2;
  line-height:1.45;
}

.tslnFaqIcon{
  position:relative;
  z-index:2;

  width:2rem;
  height:2rem;

  display:inline-flex;
  align-items:center;
  justify-content:center;
  flex-shrink:0;
  color:var(--tsln-blue);
  font-size:var(--text-md);
  transform:rotate(-90deg);
  transition:
    transform .25s ease,
    color .25s ease;
}

.tslnFaqItem.open .tslnFaqIcon{
  transform:rotate(0deg);
  color:var(--tsln-blue-dark);
}

/* =========================================
   FAQ ANSWER
========================================= */

.tslnFaqAnswer{
  position:absolute;
  top:calc(100% + -.2rem);
  left:0;
  right:0;
  z-index:60;
  padding:1rem;
  border:1px solid var(--tsln-border);
  background:var(--tsln-white);
  box-shadow:var(--shadow-sm);
}

.tslnFaqAnswer{
  animation:tslnFaqOverlay .2s ease;
}

@keyframes tslnFaqOverlay{
  from{
    opacity:0;
    transform:translateY(-5px);
  }

  to{
    opacity:1;
    transform:translateY(0);
  }
}

.tslnFaqAnswer::after{
  content:"";
  position:absolute;
  left:0;
  right:0;
  bottom:0;
  height:2px;
  background:var(--tsln-blue);
}

.tslnFaqAnswer p{
  margin:0;
  color:var(--tsln-blue-dark);
  line-height:var(--lh-normal);
  min-height:4.8rem;
}

.tslnFaqMore{
  margin-top:2rem;
  display:flex;
  justify-content:center;
}



/* MOBILE */

@media(max-width:768px){

  .tslnHelpHeroPage{
    min-height:auto;
  }

  .tslnHelpHeroInner{
    min-height:28rem;
    padding:3rem 1rem;
    grid-template-columns:1fr;
    gap:2rem;
  }

  .tslnHelpHeroLeft,
  .tslnHelpHeroRight{
    text-align:left;
  }

  .tslnHelpHeroLeft h1{
    font-size:clamp(2.2rem, 12vw, 3.25rem);
  }

  .tslnHelpHeroLeft p{
    font-size:var(--text-md);
  }

  .tslnHelpHeroRight{
    justify-self:start;
    max-width:none;
    margin-top:0;
    padding-top:1.5rem;
  }

  .tslnHelpHeroRight p{
    margin-left:0;
    font-size: var(--text-small);
    max-width:none;
  }
}



@media(max-width:768px){

  .tslnFaqHelpAd{
    position:relative;
    top:auto;
  }

  .tslnFaqHelpCards{
    gap:1rem;
  }
}

@media(max-width:768px){

    .tslnFaqHelpActions{
    display:flex;
    align-items:center;
    flex-wrap:wrap;
    gap:1rem;
    margin-top:.5rem;
    }

    .tslnFaqHelpActions a{
    position:relative;
    display:inline-flex;
    align-items:center;
    }
}

/* MOBILE */

@media(max-width:768px){


  .tslnFaqHelp{
    padding:3rem 1rem;
  }

  .tslnFaqHelpInner{
    grid-template-columns:1fr;
  }

  .tslnFaqHelpStep{
    padding:2rem 0;
  }

  .tslnFaqHelpImage{
    height:14rem;
  }

  .tslnFaqHelpAd{
    position:relative;
    top:auto;
  }

  .tslnFaqHelpAdMedia{
    height:14rem;
  }

  .tslnFaqHelpActions .tslnBtn{
    width:100%;
  }
}



/* =========================================
   MOBILE
========================================= */

@media(max-width:768px){

  .tslnFaqSection,
  .tslnFaqPage{
    padding:3rem 1rem;
    margin-top: 4em;
  }


  .tslnFaqColumns{
    grid-template-columns:1fr;
  }

  .tslnFaqAnswer p{
    min-height:auto;
  }

  .tslnFaqMore .tslnBtn{
    width:100%;
  }
}

/* ===== FILE: footer.css ===== */

/* =========================================
   11. FOOTER
========================================= */

.tslnFooter{
  width:100%;
  z-index: 5;
  top: 0;
  margin-top:auto;
  padding:4rem 0;
  background:var(--tsln-white);
  color:var(--tsln-blue-dark);
  padding-bottom: 3rem;
}

.tslnSeparatorContainer{
  width:min(100%, var(--container-max));
  margin:1rem auto;
  padding:0 var(--container-padding);
}

.tslnSeparatorContainer::after{
  content:"";
  display:block;
  height:1px;
  background:var(--tsln-border);
}

.tslnFooterInner{
  width:min(100%, var(--container-max));
  margin-inline:auto;
  padding:4rem var(--container-padding);
  display:grid;
  grid-template-columns:1.2fr 1fr 1fr 1.3fr;
  gap:3rem;
  align-items:start;
}

.tslnFooterBrand{
  display:flex;
  flex-direction:column;
  gap:1rem;
}

.tslnFooterLogo{
  width:max-content;
  margin-bottom:1rem;
  color:var(--tsln-blue);
  font-size:var(--text-xl);
  font-weight:var(--font-weight-h1);
  letter-spacing:var(--letter-tight);
  font-style:none;
  line-height:1;
  text-decoration:none;
}

.tslnFooterLogo .tslnBrandText{
  font-size:var(--text-lg);
  display: block;
  font-weight:var(--font-weight-h1);
  letter-spacing:var(--letter-tight);
  line-height:1;
}

.tslnFooterBrand p{
  margin:0;
  max-width:32rem;
  display: none;
  color:var(--tsln-blue-dark);
  font-size:var(--text-small);
  font-weight:var(--font-weight-medium);
  line-height:1.7;
}

.tslnFooterHeading{
  margin:0 0 1rem;
  color:var(--tsln-blue-dark);
  font-size:var(--text-md);
  font-weight:var(--font-weight-h1);
}

.tslnFooterLinks{
  display:flex;
  flex-direction:column;
  gap:.30rem;
}

.tslnFooterLinks a,
.tslnFooterContact a{
  width:max-content;
  color:var(--tsln-blue-dark);
  font-size:var(--text-small);
  font-weight:var(--font-weight-medium);
  text-decoration:none;
  transition:var(--transition-fast);
}

.tslnFooterLinks a:hover{
  color:var(--tsln-muted);
  text-decoration:underline;
  text-underline-offset:.25rem;
}

.tslnFooterContact{
  width:max-content;
  margin-top:4rem;
  padding:.65rem 1rem;
  border:1px solid var(--tsln-border);
  border-radius:var(--radius-full);
  box-shadow: var(--shadow-sm);
  background:var(--tsln-white);
  transition:var(--transition-fast);
}

.tslnFooterContact:hover{
  background:var(--tsln-bg-muted);
  box-shadow:var(--shadow-sm);
}

.tslnFooterSocials{
  display:flex;
  justify-content:flex-start;
  align-items:center;
  flex-wrap:wrap;
  gap:.75rem;
}

.tslnSocialIcon{
  width:3rem;
  height:3rem;
  display:inline-flex;
  align-items:center;
  justify-content:center;
  flex:0 0 3rem;
  border-radius:var(--radius-full);
  background:var(--tsln-blue);
  border:1px solid var(--tsln-blue);
  color:var(--tsln-white);
  text-decoration:none;
  transition:var(--transition-normal);
}

.tslnSocialIcon svg{
  width:1.65rem;
  height:1.65rem;
  display:block;
}

.tslnSocialIcon:hover{
  background:var(--tsln-white);
  border-color:var(--tsln-blue);
  color:var(--tsln-blue);
  transform:translateY(-2px);
}

/* =========================================
   12. FOOTER BOTTOM
========================================= */

.tslnFooterBottom{
  width:min(100%, var(--container-max));
  margin-inline:auto;
  padding:.5rem var(--container-padding);
  display:flex;
  align-items:center;
  justify-content:space-between;
  gap:3rem;
  color:var(--tsln-muted);
  font-weight: var(--font-weight-legal-medium);
  font-size:var(--text-small);
}

.tslnFooterBottom p{
  margin:0;
  font-weight: var(--font-weight-legal-medium);
}

.tslnFooterBottomLinks{
  display:flex;
  align-items:center;
  justify-content:flex-end;
  flex-wrap:wrap;
  gap:1.5rem;
}

.tslnFooterBottomLinks a{
  color:var(--tsln-blue-dark);
  text-decoration:none;
  font-weight: var(--font-weight-medium);
  transition:var(--transition-fast);
}

.tslnFooterBottomLinks a:hover{
  color:var(--tsln-muted);
  text-underline-offset:.25rem;
}

/* =========================================
   FOOTER MOBILE
========================================= */

@media(max-width:768px){

  .tslnFooterInner{
    grid-template-columns:1fr;
    padding:2rem 2rem;
    padding-bottom: 1rem;
    gap:2.25rem;
    text-align:center;
  }

  .tslnFooterBrand{
    align-items:center;
  }

  .tslnFooterLogo{
    margin-inline:auto;
    margin-bottom:.75rem;
  }

  .tslnFooterBrand p{
    max-width:100%;
    text-align:center;
  }

  .tslnFooterHeading{
    text-align:left;
    margin-top: 1rem;
  }

  .tslnFooterLinks{
    align-items:left;
  }

  .tslnFooterLinks a,
  .tslnFooterContact a{
    width:auto;
    text-align:left;
  }

  .tslnFooterContact{
    margin:1rem auto 0;
    margin-top: 3rem;
  }

  .tslnFooterSocials{
    justify-content:center;
    padding:.5rem 0;
  }

  .tslnSocialIcon{
    width:3rem;
    height:3rem;
    flex-basis:3rem;
  }

  .tslnSocialIcon svg{
    width:1.55rem;
    height:1.55rem;
  }

  .tslnFooterBottom{
    padding:1.5rem 1rem 2rem;
    flex-direction:column;
    align-items:center;
    justify-content:center;
    text-align:center;
    gap:1.5rem;
  }

  .tslnFooterBottomLinks{
    justify-content:center;
    gap:1.5rem;
  }
}

/* ===== FILE: globals.css ===== */

@import "./home-marketplace.css";
@import "./marketplace.css";


/* =========================================
   01. TSLN GLOBAL FOUNDATION
========================================= */

:root{
  --tsln-blue:#0045c5;
  --tsln-blue-hover:#003699;
  --tsln-blue-soft:rgba(0,89,255,.10);
  --tsln-blue-dark:#000000;
  --tsln-bg-application: linear-gradient(
      180deg,
      rgba(86, 117, 201, 0.178)
    );
  --tsln-bg-carousel-btn: linear-gradient(
      180deg,
      rgba(255, 255, 255, 0.445)
    );

  --tsln-bg:#ffffff;
  --tsln-bg-muted:#f1f5f9;
  --tsln-panel-soft:rgba(255,255,255,.86);
  --tsln-text:#111827;
  --tsln-muted:#6b7280;
  --tsln-white:#ffffff;
  --tsln-white-cookies:#050f1ff1;
  --tsln-border:#e5e7eb;
  --tsln-mobile-header-height:4.5rem;
  --font-main:"Inter", system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  --font-weight-text:300;
  --font-weight-legal-medium:400;
  --font-weight-medium:500;
  --font-weight-title:700;
  --font-weight-h1:900;

  --text-small:.85rem;
  --text-xs:.90rem;
  --text-md:1rem;
  --text-lg:1.125rem;
  --text-xl:clamp(1.5rem,8vw,2rem);
  --text-xxl:clamp(1.8rem,8vw,2.5rem);
  --text-hero:clamp(2.8rem,8vw,5.4rem);

  --lh-tight:1.20;
  --lh-normal:1.45rem !important;

  --letter-tight:-.06em;
  --letter-normal:0.01em;

  --container-max:84rem;
  --container-padding:1rem;

  --radius-sm:.5rem;
  --radius-xl:1.5rem;
  --radius-full:999px;

  --shadow-sm:     0 2px 6px rgba(0,0,0,.06),
                    0 6px 18px rgba(0,0,0,.08);
  
  --shadow-smd:0 21px 48px rgba(163, 167, 177, 0.568);
  --shadow-md:0 21px 52px rgba(15, 23, 42, 0.514);

  --transition-fast:.15s ease;
  --transition-normal:.25s ease;
}



/*========Mobile*/
:root{

  /* ==========================
     TYPOGRAPHY
  ========================== */

  --text-xs:0.75rem;      /* 12px */
  --text-sm:0.875rem;     /* 14px */
  --text-md:1rem;         /* 16px */
  --text-lg:1.125rem;     /* 18px */
  --text-xl:1.5rem;       /* 24px */
  --text-xxl:2rem;        /* 32px */
  --text-hero:3rem;       /* 48px */

  --lh-tight:1.15;
  --lh-normal:1.6;

}


@media (max-width:450px){

  :root{

  --header-height:4rem;
  --section-space:2.5rem;
  --card-space:.85rem;

  --text-xs:0.75rem;      /* 12px */
  --text-sm:0.875rem;     /* 14px */
  --text-md:1rem;         /* 16px */
  --text-lg:1.125rem;     /* 18px */
  --text-xl:1.5rem;       /* 24px */
  --text-xxl:2rem;  
  }

}


/* =========================================
   FORM INPUT ZOOM FIX (IOS)
========================================= */

input,
select,
textarea,
button {
  font-size:16px;
}

@media (max-width:768px){

  input,
  select,
  textarea{
    font-size:16px !important;
  }

  .tslnSearchBarBtn,
  .tslnSearchBarText,
  .tslnTNWidgetSearch input,
  .tslnSearchInput,
  .tslnHeaderSearch input{
    font-size:16px !important;
  }
}

/* =========================================
   SEARCH INPUTS
========================================= */

.tslnTNWidgetSearch input,
.tslnSearchInput,
.tslnHeaderSearch input,
.tslnSearchBarText{
  font-size:16px;
  line-height:1.4;
}

input,
textarea,
select{
  font-size:var(--text-md) !important;
}


/* =========================================
   02. RESET
========================================= */

*,
*::before,
*::after{
  box-sizing:border-box;
}

html,
body{
  margin:0;
  width:100%;
  min-height:100%;
  overflow-x:hidden;
}

html{
  scroll-behavior:smooth;
  text-size-adjust:100%;
  -webkit-text-size-adjust:100%;
}

body{
  font-family:var(--font-main);
  background:var(--tsln-bg);
  color:var(--tsln-text);
  font-size:var(--text-sm);
  font-weight:var(--font-weight-text);
  line-height:var(--lh-normal);
  letter-spacing:var(--letter-normal);
  -webkit-font-smoothing:antialiased;
  -moz-osx-font-smoothing:grayscale;
}

a{
  color:inherit;
  text-decoration:none;
}

img{
  max-width:100%;
  display:block;
}

/* =========================================
   03. GLOBAL BUTTONS
========================================= */

.tslnActions{
  margin-top:2rem;
  display:flex;
  align-items:center;
  justify-content:center;
  gap:1rem;
  flex-wrap:wrap;
}

.tslnBtn{
  min-width:12rem;
  min-height:3.2rem;
  display:inline-flex;
  align-items:center;
  justify-content:center;
  padding:0 1.5rem;
  border-radius:var(--radius-sm);
  background:var(--tsln-blue);
  color:var(--tsln-white);
  font-weight:var(--font-weight-medium);
  transition:var(--transition-fast);
}

.tslnBtn:hover{
  background:var(--tsln-blue-hover);
  transform:translateY(-1px);
}

.tslnBtn.ghost{
  background:var(--tsln-blue-dark);
}



/* =========================================
   SITE LAYOUT
========================================= */

.tslnSite{
  min-height:100vh;
  display:flex;
  flex-direction:column;
}

.tslnMain{
  flex:1;
  width:100%;
}


/* =========================================
   04. COMING SOON PAGE
========================================= */

.tslnComingPage{
  min-height:100vh;
  display:flex;
  flex-direction:column;
  background: var(--tsln-bg);
}





/* =========================================
   HERO CARD CAROUSEL
========================================= */

.tslnHeroCarousel{
  width:100%;
  padding:0 var(--container-padding) 0rem;
  overflow:hidden;
}

.tslnHeroCarouselTrack{
  width:min(100%, var(--container-max));
  margin-inline:auto;
  display:flex;
   align-items:stretch;
  justify-content:flex-start;
  gap:1rem;
  overflow-x:auto;
  overflow-y:hidden;
  padding:.25rem 0 .75rem;
  scroll-snap-type:x mandatory;
  scrollbar-width:none;
  -ms-overflow-style:none;
}

.tslnHeroCarouselTrack::-webkit-scrollbar{
  display:none;
  backgtoung:var (--tsln-white);
}

.tslnHeroCarouselCard{
  position:relative;
  flex:0 0 45%;
  height:16rem;
  overflow:hidden;
  border-radius:var(--radius-sm);
  background:var(--tsln-blue-dark);
  box-shadow: var(--shadow-sm);
  color:var(--tsln-white);
  text-decoration:none;
  scroll-snap-align:start;
}

.tslnHeroCarouselImg{
  object-fit:cover;
}

.tslnHeroCarouselOverlay{
  position:absolute;
  inset:0;

  background:
    linear-gradient(
      90deg,
      rgba(0, 0, 0, 0.26),
      rgba(0, 0, 0, 0.288)
    );
}

.tslnHeroCarouselContent{
  position:absolute;
  left:2rem;
  right:2rem;
  bottom:2rem;
  z-index:2;
}

.tslnHeroCarouselContent h1{
  margin:0 0 1rem;
  color:var(--tsln-white);
  font-size:clamp(2rem, 4vw, 3.8rem);
  font-weight:var(--font-weight-h1);
  line-height:1.05;
}


.tslnHeroCarouselCard{
  cursor:default;
}

.tslnHeroCarouselBadge{
  display:inline-flex;
  align-items:center;
  justify-content:center;
  min-height:2.5rem;
  padding:0 1rem;
  border-radius:var(--radius-sm);
  background:rgba(0, 17, 255, 0.445);
  backdrop-filter:blur(10px);
  color:var(--tsln-white);
  font-size:var(--text-xs);
  font-weight:var(--font-weight-title);
}







/* =========================================
   HERO ACTIONS
========================================= */

.tslnActions{
  width:100%;
  display:flex;
  align-items:center;
  justify-content:center;
  gap:1rem;
  flex-wrap:nowrap;
  margin-top:2rem;
}

.tslnActions .tslnBtn{
  min-width:14rem;
  flex-shrink:0;
}


/* =========================================
   HERO SEARCH TOP
========================================= */

.tslnHeroSearchTop{
  width:min(100%, var(--container-max));
  margin-inline:auto;
  padding:0rem var(--container-padding) 0rem;
  display:flex;
  justify-content:center;
}

.tslnHeroSearchTop .tslnSearchBarBtn{
  width:min(100%, 50rem);
  max-width:100%;
}

/* =========================================
   HERO SEARCH BANNER
========================================= */

.tslnHeroSearchTop{
  position:relative;
  width:min(100%, var(--container-max));
  min-height:20rem;
  margin:0 auto;
  overflow:hidden;
  background:var(--tsln-white);
}

.tslnHeroSearchBg{
  position:absolute;
  inset:0;
  z-index:0;
}

.tslnHeroSearchBgImg{
  object-fit:cover;
  object-position:center;
}

.tslnHeroSearchOverlay{
  position:absolute;
  inset:0;
  z-index:1;
  pointer-events:none;
  background:
    linear-gradient(
      180deg,
      rgb(255, 255, 255),
      rgba(255, 255, 255, 0.966),
      rgb(255,255,255)
    );
}

.tslnHeroSearchContent{
  position:relative;
  z-index:2;
  width:100%;
  min-height:20rem;
  margin:0 auto;
  padding:2rem 0 1.25rem;
  display:flex;
  flex-direction:column;
  align-items:center;
  justify-content:center;
  gap:.75rem;
}

/* =========================================
   SEARCH BAR BUTTON
========================================= */

.tslnSearchBarBtn{
  width:min(calc(100% - (var(--container-padding) * 2)), 52rem);
  max-width:45rem;
  min-height:3.2rem;
  display:flex;
  align-items:center;
  gap:.85rem;
  margin:1rem auto;
  padding:.35rem .75rem .35rem .45rem;
  border:1px solid var(--tsln-muted);
  border-radius:var(--radius-full);
  background:var(--tsln-white);
  color:var(--tsln-text);
  box-shadow:var(--shadow-md);
  cursor:pointer;
  transition:
    transform .25s ease,
    box-shadow .25s ease,
    border-color .25s ease;
}

.tslnSearchBarBtn:hover{
  transform:translateY(1px);
  border-color:var(--tsln-blue);
  box-shadow:var(--shadow-md);
}

.tslnSearchBarIcon{
  width:2.5rem;
  height:2.5rem;
  display:flex;
  align-items:center;
  justify-content:center;
  flex-shrink:0;
  border-radius:var(--radius-full);
  background:var(--tsln-blue);
  color:var(--tsln-white);
  font-size:var(--text-lg);
}

.tslnSearchBarText{
  flex:1;
  min-width:0;
  text-align:left;
  color:var(--tsln-muted);
  font-size:16px;
  font-weight:var(--font-weight-medium);
  white-space:nowrap;
  overflow:hidden;
  text-overflow:ellipsis;
}

/* =========================================
   SEARCH CATEGORY CAROUSEL - 100% WIDTH LEFT
========================================= */

.tslnSearchCategorySection{
  width:100%;
  position:relative;
  z-index:5;
  padding:0;
}

.tslnSearchCategoryInner{
  width:100%;
  max-width:none;
  margin:0;
}

.tslnSearchCategorySection .tslntitlecategorylinks{
  width:100%;
  margin:0;
  color:var(--tsln-blue-dark);
  text-align:left !important;
  font-weight:var(--font-weight-h1);
}

.tslnSearchCategoryCarousel{
  width:100%;
  display:flex;
  align-items:center;
  justify-content:flex-start;
  gap:.55rem;
  overflow-x:auto;
  padding-top: 1rem;
  overflow-y:hidden;
  scrollbar-width:none;
  -ms-overflow-style:none;
  scroll-snap-type:x proximity;
  -webkit-overflow-scrolling:touch;
}

.tslnSearchCategoryCarousel::-webkit-scrollbar{
  display:none;
}

.tslnSearchCategoryCard{
  flex:0 0 auto;
  min-width:max-content;
  min-height:2.35rem;
  display:flex;
  align-items:center;
  justify-content:center;
  padding:0 1.25rem;
  border:1px solid var(--tsln-muted);
  border-radius:var(--radius-full);
  background:var(--tsln-bg-carousel-btn);
  box-shadow:var(--shadow-lg);
  color:var(--tsln-muted);
  font-size:var(--text-xs);
  font-weight:var(--font-weight-title);
  text-decoration:none;
  white-space:nowrap;

  transition:var(--transition-fast);
  scroll-snap-align:start;
}

.tslnSearchCategoryCard:hover{
  background:var(--tsln-blue);
  color:var(--tsln-white);
  border-color:transparent;
}








/* =========================================
   EVENT FILTER BUTTONS
========================================= */

.tslnEventFilterWrap{
  width:100%;
  position:relative;
  z-index:20;
}

.tslnEventFilterBar{
  width:100%;
  display:flex;
  align-items:center;
  justify-content:flex-end;
  gap:.75rem;
}

.tslnEventFilterBtn{
  min-height:3rem;
  display:inline-flex;
  align-items:center;
  justify-content:center;
  gap:.65rem;
  padding:0 1rem;
  border:1px solid transparent;
  border-radius:var(--radius-sm);
  background:transparent;
  color:var(--tsln-blue-dark);
  font-size:16px;
  font-weight:var(--font-weight-title);
  cursor:pointer;
  white-space:nowrap;
}

.tslnEventFilterBtn:hover {
  box-shadow:var(--shadow-xs);
  background: var(--tsln-border);
  border: var(--tsln-border);
}

.tslnEventFilterBtn svg{
  color:var(--tsln-blue);
  flex-shrink:0;
}

.tslnEventFilterBtn span{
  min-width:0;
  overflow:hidden;
  text-overflow:ellipsis;
}

.tslnEventFilterArrow{
  color:var(--tsln-muted) !important;
  font-size:.8rem;
}

/* PANEL */

.tslnEventFilterBackdrop{
  position:fixed;
  inset:0;
  z-index:9999;
  border:0;
  background:rgba(0,0,0,.35);
  backdrop-filter:blur(4px);
}

.tslnEventFilterPanel{
  position:fixed;
  top:50%;
  left:50%;
  z-index:10000;
  width:min(92vw,36rem);
  max-height:86vh;
  overflow-y:auto;
  transform:translate(-50%, -50%);
  padding:1.5rem;
  border:1px solid var(--tsln-border);
  border-radius:var(--radius-sm);
  background:var(--tsln-white);
  box-shadow:0 20px 60px rgba(0,0,0,.18);
  scrollbar-width:none;
}

.tslnEventFilterPanel::-webkit-scrollbar{
  display:none;
}

.tslnEventFilterPanel h3{
  margin:0 2.5rem 1.25rem 0;
  color:var(--tsln-blue-dark);
  font-size:var(--text-lg);
  font-weight:var(--font-weight-h1);
}

.tslnEventFilterPanelClose{
  position:absolute;
  top:1rem;
  right:1rem;
  width:2rem;
  height:2rem;
  border:0;
  border-radius:var(--radius-full);
  background:var(--tsln-bg-muted);
  color:var(--tsln-blue-dark);
  cursor:pointer;
}

/* FROM / TO */
.tslnDateRangeFields{
  width:100%;
  display:flex;
  flex-direction:row;
  gap:.95rem;
  margin-bottom:1.25rem;
}

.tslnDateRangeFields button{
  flex:1 1 0;
  min-width:0;
  min-height:4rem;
  padding:.55rem 1rem;
  display:flex;
  flex-direction:column;
  justify-content:center;
  border:1px solid var(--tsln-border);
  border-radius:var(--radius-sm);
  background:var(--tsln-white);
  text-align:left;
  cursor:pointer;
}

.tslnDateRangeFields button.active{
  border-color:var(--tsln-blue);
  background:var(--tsln-blue-soft);
}

.tslnDateRangeFields span{
  color:var(--tsln-muted);
  font-size:var(--text-xs);
  font-weight:var(--font-weight-title);
}

.tslnDateRangeFields strong{
  margin-top:.25rem;
  color:var(--tsln-blue-dark);
  font-size:var(--text-sm);
  font-weight:var(--font-weight-title);
}

/* CALENDAR */

.tslnCalendarHead{
  display:flex;
  align-items:center;
  justify-content:space-between;
  margin-bottom:.75rem;
  border-bottom: 1px solid var(--tsln-bg-muted);
}

.tslnCalendarHead button{
  width:2.5rem;
  height:2.5rem;
  border:transparent;
  border-radius:var(--radius-full);
  background:transparent;
  margin-bottom: 1rem;
  color:var(--tsln-blue);
  cursor:pointer;
}

.tslnCalendarHead button:hover{
  border:1px solid var(--tsln-border);
  background:var(--tsln-bg-muted);
  color:var(--tsln-blue);
}

.tslnCalendarHead span{
  color:var(--tsln-blue-dark);
  font-weight:var(--font-weight-h1);
}

.tslnCalendarGrid{
  display:grid;
  grid-template-columns:repeat(7,minmax(0,1fr));
  gap:.35rem;
}

.tslnCalendarWeek{
  margin-bottom:.25rem;
}

.tslnCalendarWeek span{
  color:var(--tsln-muted);
  font-size:var(--text-xs);
  font-weight:var(--font-weight-title);
  text-align:center;
}


.tslnCalendarGrid > span{
  min-height:3rem;
  border:1px solid transparent;
  border-radius:var(--radius-full);
  background:transparent;
  color:var(--tsln-blue-dark);
  font-weight:var(--font-weight-medium);
}

.tslnCalendarGrid button{
  min-height:2.7rem;
  border:1px solid transparent;
  border-radius:var(--radius-full);
  background:transparent;
  color:var(--tsln-blue-dark);
  font-weight:var(--font-weight-medium);
}

.tslnCalendarGrid button.active{
  background:var(--tsln-blue);
  color:var(--tsln-white);
}

.tslnCalendarGrid button.disabled{
  opacity:.55;
  cursor:not-allowed;
  pointer-events:none;
  color:var(--tsln-muted);
}

.tslnCalendarGrid button.rangeStart{
  background:var(--tsln-blue);
  color:var(--tsln-white);
  border-color:var(--tsln-blue);
  border-radius: var(--radius-full) 0 0 var(--radius-full);
  position:relative;
  z-index:3;
}

/* Date de fin */

.tslnCalendarGrid button.rangeEnd{
  background:var(--tsln-blue);
  color:var(--tsln-white);
  border-color:var(--tsln-blue);

  border-radius:
    0
    var(--radius-full)
    var(--radius-full)
    0;

  position:relative;
  z-index:3;
}

.tslnCalendarGrid button.inRange{
  background:var(--tsln-blue-soft);
  color:var(--tsln-blue-dark);
  border-radius:0;
  position:relative;
  z-index:1;
}

.tslnCalendarGrid button.rangeStart.rangeEnd{
  border-radius:var(--radius-full);
}

/* CITY */

.tslnCityPicker{
  display:grid;
  gap:.75rem;
}

.tslnCityPicker input{
  width:100%;
  min-height:3rem;
  padding:0 1rem;
  border:1px solid var(--tsln-border);
  border-radius:var(--radius-sm);
  font-size:16px;
  outline:0;
}

.tslnCityPicker input:focus{
  border-color:var(--tsln-blue);
}

.tslnCityPicker button{
  min-height:3rem;
  border:1px solid var(--tsln-blue);
  border-radius:var(--radius-sm);
  background:var(--tsln-blue);
  color:var(--tsln-white);
  font-weight:var(--font-weight-title);
  cursor:pointer;
}

/* ACTIONS */

.tslnFilterActions{
  display:flex;
  gap:.75rem;
  margin-top:1.5rem;
}

.tslnFilterActions button{
  flex:1;
  min-height:3rem;
  border-radius:var(--radius-sm);
  font-weight:var(--font-weight-title);
  cursor:pointer;
}

.tslnFilterClear{
  border:1px solid var(--tsln-border);
  background:var(--tsln-white);
  color:var(--tsln-blue-dark);
}

.tslnFilterApply{
  border:1px solid var(--tsln-blue);
  background:var(--tsln-blue);
  color:var(--tsln-white);
}














/* =========================================
   SUGGESTED EVENTS PAGE
========================================= */

.tslnSuggestedPage{
  width:100%;
  padding-block:2rem;
}

.tslnSuggestedContainer{
  width:min(100%, var(--container-max));
  margin-inline:auto;
  padding-inline:var(--container-padding);
}

/* =========================================
   SUGGESTED ACTION
========================================= */

.tslnSuggestedAction{
  width:100%;
  padding-block:3rem;
}

.tslnSuggestedActionInner{
  width:min(100%, var(--container-max));
  margin-inline:auto;
  padding-inline:var(--container-padding);
  display:flex;
  align-items:center;
  justify-content:space-between;
  gap:2rem;
}

.tslnSuggestedAction .tslnSectionHeader{
  flex:1;
  max-width:46rem;
}

.tslnSuggestedAction .tslnSectionHeader h2{
  margin:0 0 .6rem;
  color:var(--tsln-blue-dark);
}

.tslnSuggestedAction .tslnSectionHeader p{
  color:var(--tsln-muted);
  line-height:var(--lh-normal);
}

.tslnSuggestedActionBtn{
  flex-shrink:0;
}

.tslnBtnSuggested{
  min-width:15rem;
  background:var(--tsln-white);
  border: 1px solid var(--tsln-blue);
  color:var(--tsln-blue);
}

.tslnBtnSuggested:hover{
  background:var(--tsln-white);
  color: var(--tsln-blue-dark);
  border: 1px solid var(--tsln-blue-dark);
}


.tslnSuggestedContainer{
  width:min(100%, var(--container-max));
  margin:0 auto;
}

.tslnSuggestedContainer h1{
  margin:1rem 0;
}

.tslnSuggestedContainer p{
  margin-bottom:2rem;
  width: 100%;
}

.tslnSuggestedWidget{
  width:100%;
  min-height:40rem;
  border-radius:var(--radius-lg);
  background:var(--tsln-white);
  overflow:hidden;
}




/*=========================================
    08.   IMAGEGRID PROMOTED - UI IMAGE GRID
========================================= */


.uiImageGridblock{
  width:100%;
  position: relative;
  z-index: 5;
  top: 0;
  max-width:var(--container-max);
  margin-inline:auto;
  padding-bottom: 3rem !important;
  background: var(--tsln-white);
  padding:0 var(--container-padding);
  grid-template-columns:repeat(3, 1fr);
}

.uiImageGrid{
  width:100%;
  max-width:var(--container-max);
  margin-inline:auto;
  padding:2rem ;
  display:grid;
  border-radius: var(--radius-sm);
  background:
  radial-gradient(
    circle at top left,
    rgba(0, 0, 0, 0.849),
    transparent 75%
  ),
  linear-gradient(
    185deg,
    var(--tsln-blue),
    var(--tsln-blue-dark)
  );
  grid-template-columns:repeat(3, 1fr);
  gap:1rem;
}

.uiImageGridItem{
  position:relative;
  width:100%;
  height:16rem;
  overflow:hidden;
  border-radius:var(--radius-sm);
  background:var(--tsln-bg-muted);
  box-shadow:var(--shadow-sm);
}

.uiImageGridItemFeatured{
  grid-column:span 2;
  height:16rem;
}

.uiImageGridImg{
  object-fit:cover;
  object-position:center;
  transition:transform .35s ease;
}

.uiImageGridItem:hover .uiImageGridImg{
  transform:scale(1.04);
}













/* =========================================
   09. CHRONO PROMO CARD
========================================= */

.tslnChronoWrapper{
  width:100%;
  max-width:var(--container-max);
  margin-inline:auto;
  padding:2rem var(--container-padding);
}

.tslnChronoPageBtn{
  position:relative;
  width:100%;
  min-height:18rem;
  overflow:hidden;
  border-radius:var(--radius-sm);
  background:var(--tsln-bg-application);
  box-shadow:var(--shadow-sm);
}

.tslnChronoPageBtnBg{
  position:absolute;
  inset:0;
  z-index:0;
}

.tslnChronoPageBtnImg{
  object-fit:cover;
  object-position:center;
}

.tslnChronoPageBtnOverlay{
  position:absolute;
  inset:0;
  z-index:1;
  background:
    linear-gradient(
      180deg,
      rgba(0, 0, 0, 0.041)
    );
}

.tslnChronoPageBtnContent{
  position:relative;
  z-index:2;
  min-height:18rem;
  display:grid;
  grid-template-columns:minmax(0,1fr) 12rem;
  gap:2rem;
  align-items:center;
  padding:2rem;
}

.tslnChronoText{
  max-width:42rem;
}

.tslnChronoText h2{
  margin:0;
  color:var(--tsln-blue-dark);
  font-size:clamp(1.4rem,4vw,2rem);
  font-weight:var(--font-weight-h1);
  line-height:1.05;
}

.tslnChronoText p{
  max-width:34rem;
  margin:1rem 0 0;
  color:var(--tsln-text);
  font-size:var(--text-md);
  font-weight:var(--font-weight-medium);
  line-height:var(--lh-normal);
}

.tslnChronoActions{
  display:flex;
  align-items:center;
  justify-content:flex-end;
  gap:.75rem;
  flex-wrap:nowrap;
  flex-shrink:0;
}

.tslnChronoActions .tslnBtn{
  min-width:12rem;
  min-height:3rem;
  display:flex;
  align-items:center;
  justify-content:center;
  padding:0 1rem;
}








/* =========================================
   10. SPONSORED GRID
========================================= */

.tslnSponsored{
  width:100%;
  max-width:var(--container-max);
  margin-inline:auto;
  padding:2rem var(--container-padding);
}

.tslnSponsoredHead{
  margin-bottom:1rem;
}

.tslnSponsoredGrid{
  display:grid;
   border: 1px solid var(--tsln-border);
   padding: 1rem;
   border-radius:var(--radius-sm);
  grid-template-columns:repeat(2,minmax(0,1fr));
  gap:1rem;
}

.tslnSponsoredCard{
  min-height:4.75rem;
  display:flex;
  align-items:center;
  gap:.85rem;
  padding:.55rem;
  overflow:hidden;
  border:1px solid var(--tsln-border);
  border-radius:var(--radius-sm);
  background:var(--tsln-bg-muted);
  transition:
    transform .25s ease,
    box-shadow .25s ease,
    border-color .25s ease;
}

.tslnSponsoredCard:hover{
  transform:translateY(-1px);
  border-color:var(--tsln-border);
  box-shadow:var(--shadow-md);
}

.tslnSponsoredMedia{
  position:relative;
  width:5.2rem;
  height:4.2rem;
  flex:0 0 4.2rem;
  overflow:hidden;
  border-radius:var(--radius-sm);
  background:var(--tsln-bg-muted);
}

.tslnSponsoredImg{
  object-fit:cover;
}

.tslnSponsoredInfo{
  min-width:0;
  flex:1;
}

.tslnSponsoredInfo h3{
  margin:0;
  color:var(--tsln-blue-dark);
  font-size:var(--text-md);
  font-weight:var(--font-weight-title);
  line-height:1.2;
  white-space:nowrap;
  overflow:hidden;
  text-overflow:ellipsis;
}

.tslnSponsoredInfo p{
  margin:.2rem 0 0;
  color:var(--tsln-muted);
  font-size:var(--text-xs);
  font-weight:var(--font-weight-medium);
  line-height:1.25;
  white-space:nowrap;
  overflow:hidden;
  text-overflow:ellipsis;
}

/* BUTTONS */

.tslnSponsoredBtn{
  min-width:5.75rem;
  min-height:2.45rem;
  display:inline-flex;
  align-items:center;
  justify-content:center;
  flex-shrink:0;
  padding:0 .85rem;
  border-radius:var(--radius-sm);
  border:1px solid transparent;
  color:var(--tsln-white);
  font-size:var(--text-xs);
  font-weight:var(--font-weight-title);
  text-decoration:none;
  transition:var(--transition-fast);
}

/* THEMES */

.tslnSponsoredBtn--gameon{
  background:#d46001;
  border-color:#d46001;
}

.tslnSponsoredBtn--gameon:hover{
  background:var(--tsln-white);
  color:#d46001;
}

.tslnSponsoredBtn--marykay{
  background:#d81b60;
  border-color:#d81b60;
}

.tslnSponsoredBtn--marykay:hover{
  background:var(--tsln-white);
  color:#d81b60;
}







/* =========================================
   14. TABLET
========================================= */

@media(max-width:1024px){
  .tslnFooterInner{
    grid-template-columns:1fr 1fr;
    gap:3rem;
  }

  .tslnFooterSocials{
    justify-content:flex-start;
  }

    .uiImageGrid{
    grid-template-columns:repeat(2, 1fr);
  }

  .uiImageGridItemFeatured{
    grid-column:span 2;
  }
}

/* =========================================
   15. MOBILE
========================================= */

@media(max-width:768px){
  :root{
    --container-padding:0rem;
    --text-hero:clamp(2.2rem,12vw,3rem);
  }


  /* =========================================
   MOBILE CONTAINER CONSISTENCY
========================================= */

.tslnSuggestedAction,
.tslnChronoSection,
.tslnSponsored,
.tslnFaqSection,
.tslnFaqPage,
.tslnContactPage,
.tslnAboutPage,
.tslnCategoryRail{
  padding-left:0rem;
  padding-right:0rem;
}


  .tslnHeroSearchTop{
    width:calc(100% - 1rem);
    min-height:auto;
    margin:0 auto 1rem;
  }

  .tslnHeroSearchContent{
    min-height:10rem;
    padding:0 1rem;
  }

  .tslnHeroSearchContent h2{
    font-size:clamp(1.6rem,8vw,2.2rem);
     padding-top:.55rem;
     display: none;
  }

  .tslnHeroSearchContent p{
    font-size:.95rem;
    display: none;
    margin-bottom: 0;
  }


   .tslnHeroSearchTop{
    width:100%;
    padding:0rem;
  }

  .tslnHeroSearchTop .tslnSearchBarBtn{
    margin:0;
  }

  .tslnSearchBarBtn{
    width:100%;
    max-width:none;
    flex:none;
    margin-bottom: 0rem !important;
  }

  .tslnSearchBarText{
    flex:1;
    min-width:0;
    font-size:.95rem;
    white-space:nowrap;
    overflow:hidden;
    text-overflow:ellipsis;
  }


  .tslnSearchCategorySection{
    padding:0rem;
  }

  .tslnSearchCategorySection .tslntitlecategorylinks{
    padding-top: 3rem;
    display: block !important;
  }

  .tslnSearchCategoryCarousel{
    gap:.5rem;
    padding:.75rem 0 .75rem;
  }

  .tslnSearchCategoryCard{
    min-width:6.5rem;
    min-height:2.55rem;
    padding:0 1rem;
    font-size:var(--text-small);
  }


   .tslnHeroCarousel{
    padding: .5rem 1rem;
    width: 100%;
  }

  .tslnHeroCarouselTrack{
    width:100%;
    padding:.25rem 1rem .75rem;
    gap:1rem;
  }

  .tslnHeroCarouselCard{
    flex:0 0 95%;
    height:17rem;
  }

  .tslnHeroCarouselContent{
    left:1.25rem;
    right:1.25rem;
    bottom:1.25rem;
  }

  .tslnHeroCarouselContent h1{
    font-size:clamp(1.7rem, 8vw, 2.4rem);
  }




/* Filter Btn - Mobile */


  .tslnEventFilterBar{
    justify-content:center;
    overflow-x:auto;
    gap:.6rem;
    padding:.25rem 0 .75rem;
    scrollbar-width:none;
  }

  .tslnEventFilterBar::-webkit-scrollbar{
    display:none;
  }

  .tslnEventFilterBtn{
    flex:0 0 auto;
    max-width:13rem;
  }

  .tslnEventFilterPanel{
    top:auto;
    left:0;
    right:0;
    bottom:0;
    width:auto;
    transform:none;
    border-bottom-left-radius: 0;
    border-bottom-right-radius: 0;
  }

  .tslnDateRangeFields{
    display:flex;
    flex-direction:row;
  }

  .tslnFilterActions{
    flex-direction:column;
  }




  /* Suggested */

  .tslnSuggestedPage{
    padding-block:0rem;
  }

  .tslnSuggestedAction{
    padding:1rem 1rem ;
  }

  .tslnSuggestedContainer,
  .tslnSuggestedActionInner{
    padding-inline:0;
  }

  .tslnSuggestedActionInner{
    width:100%;
    flex-direction:column;
    align-items:flex-start;
    gap:.75rem;
    padding-top:1rem;
    border-top:1px solid var(--tsln-border);
  }

  .tslnSuggestedAction .tslnSectionHeader h2{
    margin-bottom:0;
  }

  .tslnSuggestedAction .tslnSectionHeader p{
    display:none;
  }

  .tslnSuggestedActionBtn,
  .tslnBtnSuggested{
    width:100%;
  }



  /* ImageGrid */


  .uiImageGridblock{
  width:100%;
  padding: 0;
  }

  .uiImageGrid{
    padding: 4rem 1rem;
    grid-template-columns:2fr 1fr;
    border-radius: 0;
  }

  .uiImageGridItem{
    height:10rem;
  }

  .uiImageGridItemFeatured{
    grid-column:auto;
    height:10rem;
  }



/* Chrono */

.tslnChronoWrapper{
  padding:2rem 1rem;
}

.tslnChronoPageBtnContent{
  display:flex;
  flex-direction:column;
  align-items:center;
  justify-content:center;
  gap:3rem;
  padding:1rem;
}

.tslnChronoText{
  width:100%;
  gap: 0;
  text-align:center;
}

.tslnChronoText h2{
  font-size:var(--text-lg);
  padding-bottom: 0;
}

.tslnChronoText p{
  font-size:var(--text-xs);
  padding: 0 1rem;
}

.tslnChronoActions{
  width:auto;
  display:flex;
  padding: 0;
  align-items:center;
  justify-content:center;
  flex-wrap:nowrap;
  gap:.75rem;
}

.tslnChronoActions .tslnBtn{
  flex:1;
  min-width:10rem;
  min-height:3rem;
  display:flex;
  font-size: var(--text-xs);
  align-items:center;
  background: var(--tsln-blue-dark);
  justify-content:center;
}

.tslnChronoPageBtnMedia{
  height:13rem;
}




   /* Sponsored */

  .tslnSponsored{
    padding:1rem 1rem;
  }

  .tslnSponsoredGrid{
    grid-template-columns:1fr;
    gap:.75rem;
  }

  .tslnSponsoredCard{
    min-height:4.75rem;
  }

}






















/* =========================================
   LAUNCH COUNTDOWN PAGE
========================================= */

.tslnCountdown{
  width:min(100%, var(--container-max));
  margin-inline:auto;
  padding:5rem var(--container-padding);
  
}

.tslnCountdown p{
  width:100%;
  max-width:none;
  margin:0;
  color:var(--tsln-blue-dark);
  font-weight: var(--font-weight-medium);
  line-height:var(--lh-normal);
  text-align:none;
}

.tslnCountdownGrid{
  margin:3rem auto 1.5rem;
  display:grid;
  grid-template-columns:repeat(4,minmax(0,1fr));
  gap:1rem;
}

.tslnCountdownCard{
  min-height:10rem;
  display:flex;
  flex-direction:column;
  align-items:center;
  justify-content:center;
  border:1px solid var(--tsln-border);
  border-radius:var(--radius-sm);
  background:var(--tsln-white);
  box-shadow:var(--shadow-sm);
}

.tslnCountdownCard strong{
  color:var(--tsln-blue);
  font-size:clamp(2.5rem,6vw,4rem);
  font-weight:var(--font-weight-h1);
  line-height:1;
}

.tslnCountdownCard span{
  margin-top:.65rem;
  color:var(--tsln-muted);
  font-weight:var(--font-weight-title);
}

.tslnCountdownNote{
  margin-top:1.5rem !important;
  font-weight:var(--font-weight-title);
  color:var(--tsln-blue-dark) !important;
}

@media(max-width:768px){
  .tslnCountdown{
    padding:5rem 1rem;
  }

  .tslnCountdownGrid{
    grid-template-columns:repeat(2,minmax(0,1fr));
  }

  .tslnCountdownCard{
    min-height:8rem;
  }
}


/* ===== FILE: header.css ===== */

:root {
    --font-weight-text:300;
  --font-weight-medium:500;
  --font-weight-title:700;
  --font-weight-h1:900;

  --text-xs:.75rem;
  --text-md:1rem;
  --text-lg:1.25rem;
  --text-xl:clamp(1.5rem,8vw,2rem);
  --text-xxl:clamp(1.8rem,8vw,2.5rem);
  --text-hero:clamp(2.8rem,8vw,5.4rem);

  --lh-tight:1.20;
  --lh-normal:1.25;
  

  --letter-tight:-.02em;
  --letter-normal:0.01em;
  --letter-normal-logo:-.05;
}


/* =========================================
   HEADER
========================================= */

.tslnHeader {
  width:100%;
  position:sticky;
  top:0;
  z-index:20;
  background:var(--tsln-white);
  backdrop-filter:blur(14px);
}

.tslnHeaderInner{
  width:min(100%, var(--container-max));
  min-height:4.5rem;
  margin-inline:auto;
  padding:0 var(--container-padding);
  display:flex;
  align-items:center;
  gap:1rem;
  transition:
    opacity .25s ease,
    visibility .25s ease,
    transform .25s ease;
}

.tslnBrand{
  display:inline-flex;
  align-items:center;
  gap:.65rem;
  text-decoration:none;
  color:var(--tsln-blue-dark);
}

.tslnBrandIcon{
  position:relative;
  width:2.25rem;
  height:2.25rem;
  flex-shrink:0;
}

.tslnBrandIconImg{
  object-fit:contain;
}

.tslnBrand{
  display:inline-flex;
  align-items:flex-start;
  gap:.12rem;
  color:var(--tsln-blue-dark);
  text-decoration:none;
  font-weight: var(--font-weight-h1);
  white-space:nowrap;
  transition:var(--transition-fast);
}

.tslnBrand:hover{
  opacity:.99;
}

.tslnBrandText {
  font-size:var(--text-xl);
  font-weight:var(--font-weight-title);
  letter-spacing:var(--letter-tight);
  font-style:none;
  line-height:1;
}

.tslnBrandTm{
  font-size:.85rem;
  font-weight:var(--font-weight-text);
  line-height:1;
  padding:0 .2rem;
  margin-top:.2rem;
}

.tslnHeaderNav{
  display:flex;
  align-items:center;
  justify-content:flex-end;
  gap:.5rem;
  min-width:0;
}

.tslnHeaderLinkbtn{
  border: 1px solid var(--tsln-border);
  padding: .55rem .75rem;
  background: var(--tsln-blue);
  margin-right: 0;
  color: var(--tsln-white);
  font-weight: var(--font-weight-medium);
  border-radius:var(--radius-full) ;
}

.tslnHeaderLink {
  min-height:4.5rem;
  display:inline-flex;
  align-items:center;
  justify-content:center;
  padding:0 .95rem;
  color:var(--tsln-blue-dark);
  font-size:.92rem;
  font-weight:var(--font-weight-medium);
  text-decoration:none;
  white-space:nowrap;
  transition:var(--transition-fast);
}

.tslnHeaderLinkbtn:hover{
  box-shadow: var(--shadow-sm);
  background: var(--tsln-text);
  color: var(--tsln-white);
}
.tslnHeaderLink:hover{
  color:var(--tsln-muted);
}

/* =========================================
   MOBILE MENU BUTTON
========================================= */

.tslnMobileBtn{
  display:none;
  width:2.95rem;
  height:2.75rem;
  padding:0;
  margin:0;
  position:relative;
  border:0;
  border-radius:var(--radius-full);
  background:transparent;
  cursor:pointer;
  align-items:center;
  justify-content:center;
  transition:var(--transition-normal);
}

.tslnMobileBtn:hover{
  border-color:var(--tsln-bg-muted);
}

.tslnMobileBtn span{
  position:absolute;
  left:50%;
  width:1.55rem;
  height:2px;
  border-radius:var(--radius-full);
  background:var(--tsln-blue-dark);
  transform:translateX(-50%);
  transition:
    transform .25s ease,
    opacity .25s ease,
    top .25s ease;
}

.tslnMobileBtn span:nth-child(1){
  top:.85rem;
}

.tslnMobileBtn span:nth-child(2){
  top:1.35rem;
}

.tslnMobileBtn span:nth-child(3){
  top:1.85rem;
}

/* OPEN */

.tslnMobileBtn.open span:nth-child(1){
  top:1.35rem;
  transform:translateX(-50%) rotate(45deg);
}

.tslnMobileBtn.open span:nth-child(2){
  opacity:0;
}

.tslnMobileBtn.open span:nth-child(3){
  top:1.35rem;
  transform:translateX(-50%) rotate(-45deg);
}

/* =========================================
   MOBILE PANEL
========================================= */

.tslnMobilePanel{
  display:none;
}

html.tslnMenuOpen,
body.tslnMenuOpen{
  overflow:hidden !important;
  overscroll-behavior:none;
}

/* Hide main header when mobile panel is open */
body.tslnMenuOpen .tslnHeaderInner{
  opacity:0;
  visibility:hidden;
  pointer-events:none;
  transform:translateY(-100%);
}

/*======================
     Header Search BTN 
========================*/

.tslnHeaderRight{
  display:flex;
  align-items:center;
  gap:.75rem;
}

.tslnSearchIconBtn{
  width:2.75rem;
  height:2.75rem;
  min-height:4.5rem;
  display:flex;
  align-items:center;
  justify-content:center;
  border:1px solid transparent;
  border-radius:0;
  font-size: var(--text-lg);
  background:transparent;
  color:var(--tsln-blue);
  cursor:pointer;
  transition:var(--transition-normal);
}

.tslnSearchIconBtn:hover{
  background: var(--tsln-bg-muted);
  color: var(--tsln-blue);
}

.tslnHeaderDesktopGroup{
  margin-left:auto;
  display:flex;
  align-items:center;
  justify-content:flex-end;
  gap:.75rem;
  min-width:0;
}

/* =========================================
   MOBILE PANEL BASE
========================================= */

/* Keep panel full screen */
@media(max-width:768px){
  .tslnMobilePanel{
    position:fixed;
    inset:0;
    width:100%;
    height:100dvh;
    z-index:9999;
  }
}

@media(max-width:768px){

  :root{
    --tsln-mobile-header-height:4.5rem;
    --font-weight-title:700;
    --text-xl:clamp(1.5rem,8vw,1.5rem);
  }

  .tslnHeader{
    z-index:9998;
  }

  .tslnHeaderNav{
    display:none;
  }

  .tslnMobileBtn{
    display:flex;
  }

  .tslnHeaderInner{
    min-height:none;
    padding:0rem 1rem;
  }

    .tslnBrandIcon{
    width:2rem;
    height:2rem;
  }

  .tslnBrand{
  display:inline-flex;
  
}

  .tslnBrandText{
    display: none;
  }

  body.tslnMenuOpen .tslnHeaderInner {
    opacity:0;
    visibility:hidden;
    pointer-events:none;
    transform:translateY(-.5rem);
  }

  .tslnMobilePanel{
    display:block;
    position:fixed;
    inset:0;
    width:100%;
    height:100dvh;
    background:var(--tsln-white);
    opacity:0;
    visibility:hidden;
    pointer-events:none;
    transform:translateY(-12px);
    transition:
      opacity .25s ease,
      transform .25s ease,
      visibility .25s ease;

    z-index:9999;
    overflow-y:auto;
    overscroll-behavior:contain;
  }

  .tslnMobilePanel.open{
    opacity:1;
    visibility:visible;
    pointer-events:auto;
    transform:translateY(0);
  }

  .tslnMobilePanelHead{
    min-height:var(--tsln-mobile-header-height);
    display:flex;
    align-items:center;
    justify-content:space-between;
    gap:1rem;
    padding:0 1rem;
    background:var(--tsln-white);
  }

  .tslnMobilePanelClose{
    width:2.75rem;
    height:2.75rem;
    position:relative;
    display:flex;
    align-items:center;
    justify-content:center;
    border:1px solid var(--tsln-blue-dark);
    border-radius:var(--radius-full);
    background:var(--tsln-blue-dark);
    cursor:pointer;
    transition:var(--transition-normal);
  }


  .tslnMobilePanelClose:hover{
    border-color:var(--tsln-bg-muted);
  }

  .tslnMobilePanelClose span{
    position:absolute;
    left:50%;
    top:50%;
    width:1.45rem;
    height:2px;
    border-radius:var(--radius-full);
    background:var(--tsln-white);
    transition:
      transform .25s ease,
      opacity .25s ease;
  }

  .tslnMobilePanelClose span:nth-child(1){
    transform:translate(-50%, -50%) rotate(45deg);
  }

  .tslnMobilePanelClose span:nth-child(2){
    transform:translate(-50%, -50%) rotate(-45deg);
  }

  .tslnMobileSearch{
    width:100%;
    display: block;
    border-bottom:1px solid var(--tsln-border);
    background:var(--tsln-white);
  }

  .tslnMobileSearchBtn{
    width:90%;
    margin: 2rem auto;
    margin-bottom: 1rem !important;
    max-width:none;
    border: 1px solid var(--tsln-border);
    box-shadow:none;
  }

  .tslnMobileNav{
    display:flex;
    flex-direction:column;
    align-items:stretch;
    justify-content:flex-start;
    padding:1rem;
    gap:.75rem;
  }

  .tslnMobileLink{
    width:100%;
    min-height:2.55rem;
    display:flex;
    align-items:center;
    justify-content:space-between;
    padding:.5rem 0;
    background:var(--tsln-bg);
    font-size:var(--text-lg);
    color:var(--tsln-text);
    font-weight:var(--font-weight-title);
    transition:var(--transition-fast);
  }


  .tslnMobileLink:hover{
    color:var(--tsln-muted);
  }

  .tslnMobileLink:hover::after{
    color:var(--tsln-blue);
    transform:translateX(2px);
  }

  .tslnSearchOverlay{
  z-index:100000;
}

.tslnMobilePanel{
  z-index:9999;
}
}










/* =========================================
   SEARCH OVERLAY BASE
========================================= */

.tslnNoScroll,
html.tslnNoScroll,
body.tslnNoScroll{
  overflow:hidden !important;
  overscroll-behavior:none;
}

body.tslnSearchOpen .tslnHeader,
body.tslnSearchOpen .tslnMobilePanel{
  opacity:0;
  visibility:hidden;
  pointer-events:none;
  transform:translateY(-1rem);
}

.tslnSearchOverlay{
  position:fixed;
  inset:0;
  z-index:100000;
  display:flex;
  align-items:center;
  justify-content:center;
  padding:1rem;
}

.tslnSearchBackdrop{
  position:absolute;
  inset:0;
  border:0;
  background:rgba(255,255,255,.88);
  backdrop-filter:blur(12px);
  cursor:pointer;
}

.tslnSearchModal{
  position:relative;
  z-index:2;
  width:min(100%, 50rem);
  max-height:80dvh;
  overflow:hidden;
  background:var(--tsln-white);
  border:1px solid var(--tsln-border);
  border-radius:var(--radius-sm);
  box-shadow:var(--shadow-md);
}

.tslnSearchTop{
  display:flex;
  align-items:flex-start;
  justify-content:space-between;
  gap:1rem;
  padding:1.25rem;
  border-bottom:1px solid var(--tsln-border);
  background:var(--tsln-white);
}

.tslnSearchTop h2{
  margin:0;
  color:var(--tsln-blue-dark);
  font-size:var(--text-lg);
  line-height:1.1;
}


.tslnSearchClose{
  width:2.75rem;
  height:2.75rem;
  display:flex;
  align-items:center;
  justify-content:center;
  flex-shrink:0;
  border:1px solid transparent;
  border-radius:var(--radius-sm);
  background:var(--tsln-bg);
  color:var(--tsln-blue);
  font-size:1.1rem;
  cursor:pointer;
  transition:var(--transition-fast);
}

.tslnSearchClose:hover{
  background:var(--tsln-border);
  color:var(--tsln-blue-dark);
}



/* =========================================
   SEARCH BODY / BLOCK
========================================= */

.tslnSearchBody{
  width:100%;
  max-height:calc(80dvh - 5.75rem);
  overflow:auto;
  padding:1rem;
  background:var(--tsln-white);
}

.tslnSearchBlock{
  width:100%;
  min-height:1rem;
  overflow:hidden;
  border:1px solid var(--tsln-border);
  border-radius:var(--radius-sm);
  background:var(--tsln-white);
}




/* =========================================
   CUSTOM TICKETNETWORK SEARCH WIDGET
========================================= */

.tslnTNWidget{
  width:100%;
  min-height:15rem;
  padding:1rem;
  display:flex;
  flex-direction:column;
  justify-content:center;
  gap:1.25rem;
  background: var(--tsln-blue);
  color:var(--tsln-white);
}

.tslnTNWidget h3{
  margin:0;
  color:var(--tsln-white);
  font-size:clamp(1.4rem, 4vw, 2rem);
  font-weight:var(--font-weight-title);
  line-height:1.1;
  text-align:center;
}

.tslnTNWidgetSearch{
  width:min(100%, 42rem);
  margin-inline:auto;
  display:flex;
  align-items:center;
  background:var(--tsln-white);
  border:1px solid rgba(255,255,255,.22);
  border-radius:var(--radius-sm);
  overflow:hidden;
  box-shadow:var(--shadow-md);
}

.tslnTNWidgetSearch input{
  width:100%;
  min-height:2.75rem;
  padding:0 1.25rem;
  border:0;
  outline:0;
  background:var(--tsln-white);
  color:var(--tsln-blue-dark);
  font-size:var(--text-md);
  font-weight:var(--font-weight-medium);
}

.tslnTNWidgetSearch input::placeholder{
  color:var(--tsln-muted);
}

.tslnTNWidgetSearch button{
  width:3.75rem;
  height:3.75rem;

  display:flex;
  align-items:center;
  justify-content:center;
  flex-shrink:0;
  border:0;
  background:var(--tsln-white);
  color:var(--tsln-blue);
  font-size:var(--text-lg);
  cursor:pointer;
  transition:var(--transition-fast);
}

.tslnTNWidgetSearch button:hover{
  background:var(--tsln-blue);
  color:var(--tsln-white);
}

.tslnTNWidgetPowered{
  margin:0;
  color:rgba(255,255,255,.82);
  font-size:var(--text-xs);
  font-weight:var(--font-weight-medium);
  text-align:center;
}

.tslnTNWidgetPowered strong{
  color:var(--tsln-white);
  font-weight:var(--font-weight-title);
}



/* =========================================
   LEGACY WIDGET SAFE SUPPORT
========================================= */

.tslnSearchIframe{
  width:100%;
  height:26rem;
  display:block;
  border:0;
  background:var(--tsln-white);
}

.tslnSearchWidget,
.tslnWidgetMount{
  width:100%;
  min-height:15rem;
  display:block !important;
  position:relative;
  overflow:visible;
  background:var(--tsln-white);
}

/* =========================================
   MOBILE SEARCH OVERLAY
========================================= */

@media(max-width:768px){

  .tslnSearchOverlay{
    align-items:flex-start;
    padding:0;
  }

  .tslnSearchBackdrop{
    background:var(--tsln-white);
    backdrop-filter:none;
  }

  .tslnSearchModal{
    width:100%;
    min-height:100dvh;
    max-height:100dvh;
    border:0;
    border-radius:0;
    box-shadow:none;
  }

  .tslnSearchTop{
    padding:1rem;
  }

  .tslnSearchTop h2{
    font-size:var(--text-lg);
  }

  .tslnSearchBody{
    max-height:calc(100dvh - 5.25rem);
    padding:1rem;
    overflow:auto;
  }

  .tslnSearchBlock{
    min-height:15rem;
  }

  .tslnTNWidget{
    min-height:16rem;
    padding:1.25rem;
  }

  .tslnTNWidgetSearch{
    width:100%;
  }

  .tslnTNWidgetSearch input{
    min-height:3.5rem;
    font-size:.95rem;
  }

  .tslnTNWidgetSearch button{
    width:3.5rem;
    height:3.5rem;
  }

  .tslnSearchWidget,
  .tslnWidgetMount{
    min-height:15rem;
  }

  .tslnSearchIframe{
    height:26rem;
  }
}

/* ===== FILE: HeaderScroll.css ===== */

/* =========================================
   HEADER SCROLL SEPARATOR
========================================= */

.tslnHeaderSeparator {
  width: min(100%, var(--container-max));
  margin-inline: auto;
  padding: 0;
  z-index: 10; /* au-dessus des sections de contenu (z-index: 1+) */
  top: 0;
  background: var(--tsln-white);
  display: flex;
  align-items: center;
  gap: .35rem;
  opacity: var(--tsln-separator-progress);
  transform: translateX(
    calc((1 - var(--tsln-separator-progress)) * -1rem)
  );
  transition:
    opacity .95s ease,
    transform .8s ease;
}

.tslnHeaderSeparatorLine{
  height:40px;
  flex:1;
  background:var(--tsln-blue);
  opacity:1;
  transform:scaleX(var(--tsln-separator-progress));
  transform-origin:left center;
  transition:transform .8s ease;
}

.tslnHeaderSeparatorDate{
  flex-shrink:0;
  color:var(--tsln-blue-dark);
  opacity:1;
  font-size:var(--text-xs);
  font-weight:var(--font-weight-medium);
  white-space:nowrap;
  transform:translateX(
    calc((1 - var(--tsln-separator-progress)) * .65rem)
  );

  transition:transform .8s ease;
}

body.tslnMenuOpen .tslnHeaderSeparator,
body.tslnSearchOpen .tslnHeaderSeparator{
  display:none;
}

@media(max-width:768px){
  .tslnHeaderSeparator{
    padding:0;
    gap:.2rem;
  }

  .tslnHeaderSeparatorDate{
    max-width:10rem;
    overflow:hidden;
    padding:0;
    text-overflow:ellipsis;
  }
}

/* ===== FILE: heroUi-page.css ===== */

/* =========================================
   01. TSLN GLOBAL FOUNDATION
========================================= */

:root{
  --tsln-blue:#0045c5;
  --tsln-blue-hover:#003699;
  --tsln-blue-soft:rgba(0,89,255,.10);
  --tsln-blue-dark:#000000;

  --tsln-bg:#ffffff;
  --tsln-bg-muted:#f1f5f9;
  --tsln-panel-soft:rgba(255,255,255,.86);

  --tsln-text:#111827;
  --tsln-muted:#6b7280;
  --tsln-white:#ffffff;
  --tsln-border:#e5e7eb;

  --tsln-mobile-header-height:4.5rem;

  --font-main:"Inter", system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  --font-weight-text:300;
  --font-weight-medium:500;
  --font-weight-title:700;
  --font-weight-h1:900;

  --text-xs:.75rem;
  --text-sm:.875rem;
  --text-md:1rem;
  --text-lg:1.125rem;
  --text-xl:clamp(1.5rem,8vw,2rem);
  --text-xxl:clamp(1.8rem,8vw,2.5rem);
  --text-hero:clamp(2.8rem,8vw,5.4rem);

  --lh-tight:1.15;
  --lh-normal:1.6;

  --letter-tight:-.06em;
  --letter-normal:.01em;

  --container-max:84rem;
  --container-padding:3rem;

  --radius-sm:.5rem;
  --radius-xl:1.5rem;
  --radius-full:999px;

  --shadow-sm:0 6px 18px rgba(15,23,42,.06);
  --shadow-md:0 12px 32px rgba(15,23,42,.08);

  --transition-fast:.15s ease;
  --transition-normal:.25s ease;
}

/* =========================================
   UI HERO UNIVERSAL
========================================= */

.uiHero{
  position:relative;
  z-index:5;
  width:100%;
  overflow:hidden;
  background: var(--tsln-bg-muted);
}

.uiHeroWithImage{
  min-height:40vh;
  padding:5rem var(--container-padding);
  border: 2rem var(--tsln-muted);
  color:var(--tsln-white);
}

.uiHeroPlain{
  min-height:18rem;
  padding:2rem var(--container-padding);
  color:var(--tsln-blue-dark);
  background:var(--tsln-white);
}

/* =========================================
   UI HERO INNER BACKGROUND IMAGE
========================================= */

.uiHeroInner{
  position:relative;
  z-index:2;
  max-width: 60rem;
  width:min(100%, var(--container-max));
  min-height:inherit;
  margin-inline:auto;
  padding:3rem var(--container-padding) 3rem;
  display:flex;
  flex-direction:column;
  justify-content:center;
  overflow:hidden;
  border-radius:var(--radius-sm);
  background:var(--tsln-white);
}

.uiHeroBg{
  position:absolute;
  inset:0;
  z-index:2;
  padding: 1rem;
  border-radius: var(--radius-sm);
  border: 3px solid var(--tsln-muted);
  background:var(--tsln-white);
}

.uiHeroImg{
  object-fit:cover;
  border-radius: var(--radius-sm);
  object-position:center;
}

.uiHeroOverlay{
  position:absolute;
  inset:0;
  z-index:1;
  pointer-events:none;
  background:
    linear-gradient(
      180deg,
      rgba(255, 255, 255, 0.075),
      rgba(255, 255, 255, 0.144)
    ),
    radial-gradient(
      circle at 20% 20%,
      rgba(0,69,197,.18),
      transparent 34%
    );
}

.uiHeroContent{
  position:relative;
  z-index:2;
  min-height:inherit;
  display:flex;
  flex-direction:column;
  justify-content:center;
}

.uiHero-center .uiHeroInner,
.uiHero-center .uiHeroContent{
  align-items:center;
  text-align:center;
}

.uiHero-left .uiHeroInner,
.uiHero-left .uiHeroContent{
  align-items:flex-start;
  text-align:left;
}

/* =========================================
   UI HERO TITLE MARK
========================================= */

.uiHeroTitleRow{
  width:100%;
  display:flex;
  align-items:center;
  justify-content:center;
  gap:1.25rem;
}

.uiHero-left .uiHeroTitleRow{
  justify-content:flex-start;
}

.uiHeroMark{
  width:6rem;
  height:6rem;
  display:inline-flex;
  align-items:center;
  justify-content:center;
  flex-shrink:0;
  border:1px solid var(--tsln-blue);
  border-radius:var(--radius-full);
  background:var(--tsln-blue);
  color:var(--tsln-white);
  font-size:5rem;
  font-weight:var(--font-weight-title);
  font-style:italic;
  line-height:1;

  backdrop-filter:blur(10px);
}

.uiHeroSlash{
  width:1px;
  height:4.5rem;
  flex-shrink:0;
  background:var(--tsln-blue-dark);
  transform:rotate(18deg);
}

.uiHeroTitleRow h1{
  margin:0;
}

.uiHero h1{
  width:min(100%, 64rem);
  margin:0;
  color:var(--tsln-blue-dark);
  font-size:var(--text-xxl);
  font-weight:var(--font-weight-h1);
  line-height:var(--lh-tight);
  letter-spacing:var(--letter-normal);
}

.uiHeroWithImage h1{
  color:var(--tsln-blue-dark);
}

.uiHeroPlain h1{
  color:var(--tsln-blue-dark);
}

.uiHero p{
  width:min(100%, 44rem);
  margin:1rem 0 0;
  color:var(--tsln-blue-dark);
  font-size:var(--text-md);
  font-weight:var(--font-weight-medium);
  line-height:1.7;
}

.uiHero-center p{
  margin-inline:auto;
}

.uiHeroActions{
  width:100%;
  margin-top:2rem;

  display:flex;
  gap:1rem;
  flex-wrap:wrap;
}

.uiHero-center .uiHeroActions{
  justify-content:center;
}

.uiHero-left .uiHeroActions{
  justify-content:flex-start;
}

/* =========================================
   BREADCRUMBS FOOTER
========================================= */

.uiBreadcrumbs{
  position:absolute;
  left:var(--container-padding);
  right:var(--container-padding);
  bottom:1.5rem;
  z-index:3;

  display:flex;
  align-items:center;
  justify-content:flex-start;
  flex-wrap:wrap;
  gap:.45rem;
  color:var(--tsln-blue-dark);
  font-size:var(--text-xs);
  font-weight:var(--font-weight-medium);
}

.uiBreadcrumbs a{
  color:inherit;
  opacity:.78;
  text-decoration:none;
  transition:var(--transition-fast);
}

.uiBreadcrumbs a:hover{
  opacity:1;
}

.uiBreadcrumbItem{
  display:inline-flex;
  align-items:center;
  gap:.45rem;
}

.uiBreadcrumbSep{
  opacity:.45;
}

.uiBreadcrumbItem span[aria-current="page"]{
  opacity:1;
  font-weight:var(--font-weight-title);
}

/* =========================================
   UI HERO DECORATIVE MARK
========================================= */

.uiHeroMarkDecor{
  position:absolute;
  top:50%;
  right:3rem;
  transform:translateY(-50%);
  z-index:2;
  display:flex;
  align-items:center;
  justify-content:center;
  pointer-events:none;
}

.uiHeroMarkDecor span{
  font-size:35rem;
  font-style:italic;
  font-weight:var(--font-weight-title);
  color:transparent;
  -webkit-text-fill-color:var(--tsln-bg-muted);
  -webkit-text-stroke:2px var(--tsln-white);
  text-shadow:var(--shadow-md);
  opacity:.22;
}

/* =========================================
   MOBILE
========================================= */

@media(max-width:768px){

  :root{
    --container-padding:1rem;
    --text-xl:1.5rem;
    --text-xxl:2rem;
    --text-hero:3rem;
  }

  .uiHeroWithImage,
  .uiHeroPlain{
    padding:4rem 0;
    min-height:30vh;
  }

  .uiHeroInner{
    width:100%;
    max-width: 20rem;
    min-height:inherit;
    padding:0rem 1rem 2rem;
  }

  .uiHeroTitleRow{
    justify-content:flex-start;
    gap:.75rem;
  }

  .uiHero-center .uiHeroTitleRow{
    justify-content:center;
  }

  .uiHeroMark{
    width:4.75rem;
    height:4.75rem;
    font-size:4.1rem;
  }

  .uiHeroSlash{
    display:none;
  }

  .uiHero h1{
    font-size:var(--text-xl);
  }

  .uiHero p{
    font-size:var(--text-sm);
  }

  .uiHeroActions{
    flex-direction:column;
  }

  .uiHeroActions .tslnBtn,
  .uiHeroActions button,
  .uiHeroActions a{
    width:100%;
  }

  .uiBreadcrumbs{
    left:1rem;
    right:1rem;
    bottom:1rem;
    font-size:var(--text-xs);
  }

  .uiHeroMarkDecor{
    right:1rem;
    width:7rem;
    height:7rem;
    opacity:.45;
  }

  .uiHeroMarkDecor span{
    font-size:22rem;
  }
}

/* ===== FILE: home-marketplace.css ===== */

/* Missing tokens (repairs chips, search bar, filter, widget) */
:root{
  --radius-lg:1rem;
  --shadow-xs:0 1px 2px rgba(0,0,0,.05);
  --shadow-lg:0 18px 40px rgba(15,23,42,.12);
}

/* EVENT SECTION */
.tslnEventSection{ width:min(100%, var(--container-max)); margin-inline:auto; padding:1.75rem var(--container-padding); }
.tslnEventSectionHead{ display:flex; align-items:baseline; justify-content:space-between; gap:1rem; margin-bottom:1rem; }
.tslnEventSectionHead h2{ margin:0; color:var(--tsln-blue-dark); font-size:var(--text-lg); font-weight:var(--font-weight-h1); line-height:1.1; }
.tslnEventSectionHead a{ flex-shrink:0; color:var(--tsln-blue); font-size:var(--text-xs); font-weight:var(--font-weight-title); }
.tslnEventGrid{ width:100%; display:grid; grid-template-columns:repeat(auto-fill, minmax(15rem, 1fr)); gap:1rem; }

/* EVENT CARD */
.tslnEventCard{ position:relative; display:flex; flex-direction:column; overflow:hidden; border:1px solid var(--tsln-border); border-radius:var(--radius-sm); background:var(--tsln-white); color:inherit; text-decoration:none; transition:transform .25s ease, box-shadow .25s ease, border-color .25s ease; }
.tslnEventCard:hover{ transform:translateY(-2px); border-color:var(--tsln-blue); box-shadow:var(--shadow-sm); }
.tslnEventCardMedia{ position:relative; width:100%; height:8.5rem; overflow:hidden; background:linear-gradient(135deg, var(--tsln-blue), var(--tsln-blue-dark)); }
.tslnEventCardImg{ object-fit:cover; object-position:center; transition:transform .35s ease; }
.tslnEventCard:hover .tslnEventCardImg{ transform:scale(1.04); }
.tslnEventCardDate{ position:absolute; left:.6rem; bottom:-.95rem; width:2.85rem; overflow:hidden; border:1px solid var(--tsln-border); border-radius:var(--radius-sm); background:var(--tsln-white); text-align:center; z-index:2; }
.tslnEventCardDate small{ display:block; padding:.12rem 0; background:var(--tsln-blue); color:var(--tsln-white); font-size:.62rem; font-weight:var(--font-weight-title); letter-spacing:.05em; }
.tslnEventCardDate strong{ display:block; padding:.18rem 0; color:var(--tsln-blue-dark); font-size:1.05rem; font-weight:var(--font-weight-h1); }
.tslnEventCardBody{ padding:1.45rem .85rem .9rem; display:flex; flex-direction:column; gap:.3rem; }
.tslnEventCardTitle{ margin:0; color:var(--tsln-blue-dark); font-size:var(--text-sm); font-weight:var(--font-weight-title); line-height:1.25; display:-webkit-box; -webkit-line-clamp:2; -webkit-box-orient:vertical; overflow:hidden; }
.tslnEventCardMeta{ display:flex; align-items:center; gap:.35rem; color:var(--tsln-muted); font-size:var(--text-xs); font-weight:var(--font-weight-medium); white-space:nowrap; overflow:hidden; text-overflow:ellipsis; }
.tslnEventCardFooter{ margin-top:.55rem; padding-top:.55rem; border-top:1px solid var(--tsln-border); display:flex; align-items:center; justify-content:space-between; }
.tslnEventCardFrom{ color:var(--tsln-muted); font-size:var(--text-xs); }
.tslnEventCardPrice{ color:var(--tsln-blue); font-size:var(--text-md); font-weight:var(--font-weight-h1); }

@media(max-width:768px){
  .tslnEventSection{ padding:1.25rem 1rem; }
  .tslnEventGrid{ grid-template-columns:repeat(auto-fill, minmax(9.5rem, 1fr)); gap:.75rem; }
  .tslnEventCardMedia{ height:7rem; }
}

/* ===== FILE: marketplace.css ===== */

/* Ticketsoccers - funnel + components. Neutral colors; map to your tokens if needed. */
.tsln-container { max-width: 56rem; margin: 0 auto; padding: 2rem 1rem; }
.tsln-container--wide { max-width: 72rem; }
.tsln-container--narrow { max-width: 36rem; margin: 0 auto; padding: 4rem 1rem; text-align: center; }
.tsln-grid { display: grid; gap: 2rem; }
@media (min-width: 1024px) { .tsln-grid--checkout, .tsln-grid--seats { grid-template-columns: 2fr 1fr; } }
.tsln-stack { display: flex; flex-direction: column; gap: .75rem; }
.tsln-title { font-size: 1.5rem; font-weight: 600; }
.tsln-subtitle { margin-top: .25rem; color: #6b7280; }
.tsln-section { margin-top: 2rem; }
.tsln-section__head { font-weight: 500; margin-bottom: .75rem; }
.tsln-muted { color: #6b7280; }
.tsln-mt { margin-top: 1rem; }
.tsln-mb { margin-bottom: 1rem; }
.tsln-btn { display: inline-block; border: 0; border-radius: .5rem; padding: .625rem 1.25rem; font-weight: 500; cursor: pointer; }
.tsln-btn--primary { background: #059669; color: #fff; }
.tsln-btn--primary:hover { background: #047857; }
.tsln-btn--primary:disabled { opacity: .5; cursor: not-allowed; }
.tsln-btn--dark { background: #0f172a; color: #fff; }
.tsln-btn--sm { padding: .5rem 1rem; font-size: .875rem; }
.tsln-ticket { display: flex; align-items: center; justify-content: space-between; width: 100%; text-align: left; border: 1px solid #e5e7eb; border-radius: .5rem; padding: 1rem; background: #fff; cursor: pointer; }
.tsln-ticket:hover { border-color: #d1d5db; }
.tsln-ticket--selected { border-color: #10b981; box-shadow: 0 0 0 1px #10b981; }
.tsln-ticket__title { font-weight: 500; }
.tsln-ticket__meta { margin-top: .25rem; display: flex; align-items: center; gap: .5rem; font-size: .75rem; color: #6b7280; }
.tsln-badge { display: inline-flex; align-items: center; border-radius: .25rem; padding: .125rem .5rem; font-size: .75rem; font-weight: 500; }
.tsln-badge--hot { background: #fee2e2; color: #991b1b; }
.tsln-badge--soldout { background: #e5e7eb; color: #374151; }
.tsln-badge--featured { background: #fef3c7; color: #92400e; }
.tsln-badge--neutral { background: #f1f5f9; color: #334155; }
.tsln-price { font-weight: 500; }
.tsln-price--stacked { display: flex; flex-direction: column; line-height: 1.1; }
.tsln-price__total { font-weight: 600; }
.tsln-price__breakdown { font-size: .75rem; color: #6b7280; }
.tsln-selector { display: flex; align-items: center; gap: 1rem; border: 1px solid #e5e7eb; border-radius: .5rem; padding: 1rem; }
.tsln-selector__main { flex: 1; }
.tsln-selector__qty { border: 1px solid #d1d5db; border-radius: .375rem; padding: .25rem .5rem; }
.tsln-stepper { display: flex; align-items: center; gap: .5rem; margin-bottom: 2rem; padding: 0; list-style: none; }
.tsln-stepper__item { display: flex; flex: 1; align-items: center; gap: .5rem; }
.tsln-stepper__num { display: flex; height: 1.75rem; width: 1.75rem; align-items: center; justify-content: center; border-radius: 9999px; font-size: .875rem; background: #e5e7eb; color: #6b7280; }
.tsln-stepper__item--done .tsln-stepper__num { background: #059669; color: #fff; }
.tsln-stepper__label--muted { color: #9ca3af; }
.tsln-stepper__bar { margin-left: .5rem; height: 1px; flex: 1; background: #e5e7eb; }
.tsln-summary { border: 1px solid #e5e7eb; border-radius: .5rem; padding: 1.25rem; }
.tsln-summary__title { margin-bottom: 1rem; font-weight: 500; }
.tsln-summary__list { margin: 0; padding: 0; list-style: none; display: flex; flex-direction: column; gap: .5rem; font-size: .875rem; }
.tsln-summary__row { display: flex; justify-content: space-between; }
.tsln-summary__row--muted { color: #6b7280; }
.tsln-summary__row--total { padding-top: .5rem; font-size: 1rem; font-weight: 600; }
.tsln-summary hr { margin: 1rem 0; border: 0; border-top: 1px solid #e5e7eb; }
.tsln-frame { height: 420px; width: 100%; border: 1px solid #e5e7eb; border-radius: .5rem; }
.tsln-seatmap { height: 480px; width: 100%; border: 1px solid #e5e7eb; border-radius: .5rem; }
.tsln-skeleton { background: #f3f4f6; animation: tsln-pulse 1.5s ease-in-out infinite; }
@keyframes tsln-pulse { 50% { opacity: .5; } }
.tsln-eticket { display: flex; align-items: center; gap: 1rem; border: 1px solid #e5e7eb; border-radius: .5rem; padding: 1.25rem; }
.tsln-eticket__qr { height: 7rem; width: 7rem; }
.tsln-eticket__order { font-size: .875rem; color: #6b7280; }
.tsln-order { display: block; border: 1px solid #e5e7eb; border-radius: .5rem; padding: 1rem; text-decoration: none; color: inherit; }
.tsln-order:hover { border-color: #d1d5db; }
.tsln-order__head, .tsln-order__meta { display: flex; justify-content: space-between; }
.tsln-order__meta { margin-top: .25rem; font-size: .875rem; color: #6b7280; }
.tsln-form-grid { display: grid; grid-template-columns: 1fr 1fr; gap: .75rem; }
.tsln-field { border: 1px solid #d1d5db; border-radius: .375rem; padding: .5rem .75rem; }
.tsln-field--full { grid-column: span 2; }
.tsln-confirm__check { margin: 0 auto 1rem; display: flex; height: 3.5rem; width: 3.5rem; align-items: center; justify-content: center; border-radius: 9999px; background: #d1fae5; color: #047857; font-size: 1.5rem; }

/* ===== FILE: not-found.css ===== */


/* =========================================
   13. 404 PAGE
========================================= */

.tsln404Page{
  min-height:100vh;
  display:flex;
  align-items:center;
  padding-top: 4em;
  justify-content:center;
  padding:var(--container-padding);
  background:var(--tsln-bg);
}

.tsln404Inner{
  width:min(100%,70rem);
  text-align:center;
}


.tsln404Inner h1{
  margin:1.25rem 0 1rem;
  color:var(--tsln-blue-dark);
  font-size:var(--text-xxl);
  font-weight:var(--font-weight-title);
  line-height:var(--lh-tight);
  letter-spacing:var(--letter-tight);
}

.tsln404Inner p{
  max-width:100%;
  margin:0 auto;
  color:var(--tsln-blue-dark);
  font-size:var(--text-md);
  font-weight: var(--font-weight-medium);
  line-height:var(--lh-normal);
}

.tsln404Help{
  margin-top:2rem;
}

.tsln404Tools{
  padding: 2rem 0;
}
/* Search button inside 404 page */

.tsln404Page .tsln404SearchBtn{
  width:min(100%, 34rem);
  max-width:34rem;
  min-height:4.15rem;
  margin:1.5rem auto 0;
  border:1px solid var(--tsln-blue);
  border-radius:var(--radius-full);
  background:var(--tsln-white);
  color:var(--tsln-blue-dark);
  box-shadow:var(--shadow-sm);
}

.tsln404Page .tsln404SearchBtn:hover{
  transform:translateY(-1px);
  box-shadow:var(--shadow-md);
}

.tsln404Page .tsln404SearchBtn .tslnSearchBarIcon{
  width:3.2rem;
  height:3.2rem;
  color:var(--tsln-white);
}

.tsln404Page .tsln404SearchBtn .tslnSearchBarText{
  color:var(--tsln-muted);
}

.tsln404Page .tslnActions .tsln404SearchBtn{
  flex:0 1 34rem;
}

.tsln404DetailsBtn{
  min-height:3rem;
  padding:0 1.2rem;
  border:1px solid var(--tsln-bg-muted);
  border-radius:var(--radius-sm);
  background:var(--tsln-border);
  color:var(--tsln-blue-dark);
  font-size: var(--text-md);
  font-weight:var(--font-weight-medium);
  cursor:pointer;
  transition:var(--transition-fast);
}

.tsln404DetailsBtn:hover{
  border-color:var(--tsln-muted);
  background: var(--tsln-muted);
  color:var(--tsln-white);
}

.tsln404Details{
  margin:1.5rem auto 0;
  padding:1.5rem;
  max-width:48rem;
  text-align:left;
  border-top: 1px solid var(--tsln-muted);
  background:var(--tsln-white);
  box-shadow:var(--shadow-xs);
}

.tsln404Details h2,
.tsln404Details h3{
  margin:0 0 .75rem;
  color:var(--tsln-blue-dark);
}

.tsln404Details h3{
  margin-top:1.5rem;
}

.tsln404ErrorTitle{
  font-weight:var(--font-weight-medium);
}

.tsln404Details code{
  display:inline-flex;
  margin-top:1rem;
  padding:.75rem .75rem;
  border-radius:var(--radius-sm);
  background:var(--tsln-bg-muted);
  color:var(--tsln-blue-dark);
  font-weight:var(--font-weight-medium);
}

.tsln404Details ul{
  margin:1rem 0 0 1.25rem;
  padding:0;
}

.tsln404Details li{
  margin-bottom:.35rem;
  color:var(--tsln-muted);
  line-height:1.6;
}


/* MOBILE */

@media(max-width:768px){

  .tslnActions{
    flex-direction:column;
    align-items:stretch;
    gap:.75rem;
  }

  .tslnActions .tslnBtn{
    width:100%;
    min-width:0;
  }

}

/* ===== FILE: publicity.css ===== */

/* =========================================
   PUBLICITY CARD
========================================= */

.tslnPublicity{
  width:100%;
  padding:1rem var(--container-padding);
}

.tslnPublicityInner{
  width:min(100%, var(--container-max));
  min-height:25rem;
  margin-inline:auto;
  position:relative;
  overflow:hidden;
  display:flex;
  flex-direction:column;
  align-items:center;
  justify-content:center;
  gap:1.25rem;
  padding:1rem;
  border-radius:var(--radius-sm);
  color:var(--tsln-white);
  box-shadow:var(--shadow-sm);
  animation:tslnPublicityBackground 100s ease-in-out infinite;
}

/* =========================================
   PUBLICITY BACKGROUND ROTATION
========================================= */

@keyframes tslnPublicityBackground{

  /* BLUE */

  0%,
  19%{
    background:
      radial-gradient(
        circle at 15% 20%,
        rgba(0,69,197,.65),
        transparent 36%
      ),
      linear-gradient(
        135deg,
        #041d63,
        #0b51d8
      );
  }

  /* PURPLE */

  20%,
  39%{
    background:
      radial-gradient(
        circle at 20% 25%,
        rgba(127,86,217,.65),
        transparent 36%
      ),
      linear-gradient(
        135deg,
        #2d1457,
        #7f56d9
      );
  }

  /* RED */

  40%,
  59%{
    background:
      radial-gradient(
        circle at 20% 20%,
        rgba(220,38,38,.65),
        transparent 36%
      ),
      linear-gradient(
        135deg,
        #5f1010,
        #dc2626
      );
  }

  /* GREEN */

  60%,
  79%{
    background:
      radial-gradient(
        circle at 18% 22%,
        rgba(22,163,74,.65),
        transparent 36%
      ),
      linear-gradient(
        135deg,
        #064e3b,
        #16a34a
      );
  }

  /* GOLD */

  80%,
  100%{
    background:
      radial-gradient(
        circle at 20% 20%,
        rgba(245,158,11,.65),
        transparent 36%
      ),
      linear-gradient(
        135deg,
        #78350f,
        #f59e0b
      );
  }
}

.tslnPublicityInner::after{
  content:"";
  position:absolute;
  inset:0;
  pointer-events:none;
  background:
    linear-gradient(
      120deg,
      transparent,
      rgba(255,255,255,.055),
      transparent
    );
  transform:translateX(-120%);
  animation:tslnPublicityShine 20s ease-in-out infinite;
}

.tslnPublicityBadge{
  display:none;
}

.tslnPublicityWords{
  position:relative;
  z-index:2;
  min-height:3.25rem;
  display:flex;
  align-items:center;
  justify-content:center;
  width:100%;
  text-align:center;
  font-size:clamp(2.25rem, 6vw, 5rem);
  font-weight:var(--font-weight-title);
  line-height:1;
  letter-spacing:var(--letter-tight);
}

.tslnPublicityWords span{
  display:inline-block;
  color:var(--tsln-white);
  animation:tslnPublicityWord 0.30s ease both;
}

.tslnPublicityMessage{
  position:relative;
  z-index:2;
  min-height:2.5rem;
  display:flex;
  align-items:center;
  justify-content:center;
  width:100%;
}

.tslnPublicityMessage p{
  margin:0;
  max-width:44rem;
  color:rgba(255,255,255,.82);
  font-size:var(--text-lg);
  font-weight:var(--font-weight-medium);
  line-height:1.4;
  text-align:center;
  animation:tslnPublicityText 0.03s ease both;
}

.tslnPublicityPowered{
  position:absolute;
  right:1.25rem;
  bottom:1.25rem;
  z-index:2;
  display:flex;
  align-items:center;
  justify-content:flex-end;
  gap:.65rem;
  color:rgba(255,255,255,.66);
  font-size:var(--text-xs);
  text-align:right;
}

.tslnPublicityPowered .tslnBrandText{
  font-size:1rem;
  display: block;
  color:var(--tsln-white);
}

.tslnPublicityPowered .tslnBrandIcon{
  display: none;
}

.tslnPublicityPowered .tslnBrandTm{
  color:var(--tsln-white);
}

/* =========================================
   ANIMATIONS
========================================= */

@keyframes tslnPublicityWord{
  0%{
    opacity:0;
    transform:translateY(.3rem);
    filter:blur(8px);
  }

  12%{
    opacity:1;
    transform:translateY(0);
    filter:blur(0);
  }

  80%{
    opacity:1;
    transform:translateY(0);
    filter:blur(0);
  }

  100%{
    opacity:1;
    transform:translateY(0);
    filter:blur(0);
  }
}

@keyframes tslnPublicityText{
  0%{
    opacity:0;
    transform:translateY(.3rem);
  }

  15%{
    opacity:1;
    transform:translateY(0);
  }

  80%{
    opacity:1;
    transform:translateY(0);
  }

  100%{
    opacity:1;
    transform:translateY(0);
  }
}

@keyframes tslnPublicityShine{
  0%{
    transform:translateX(-120%);
  }

  35%{
    transform:translateX(120%);
  }

  100%{
    transform:translateX(120%);
  }
}

/* =========================================
   MOBILE
========================================= */

@media(max-width:768px){

  .tslnPublicity{
    padding:1rem 1rem;
  }

  .tslnPublicityInner{
    min-height:15rem;
    padding:0;
  }

  .tslnPublicityWords{
    min-height:1rem;
    margin-bottom: 0;
    font-size:clamp(1.6rem, 12vw, 2.65rem);
  }

  .tslnPublicityMessage p{
    font-size:var(--text-md);
    margin-top: 0;
  }

  .tslnPublicityPowered{
    position:relative;
    right:auto;
    bottom:auto;
    justify-content:center;
    text-align:center;
    margin-top:1rem;
  }
}

/* ===== FILE: terms-of-use.css ===== */




/* =========================================
   LEGAL PAGES
   Terms / Privacy
========================================= */

.tslnLegalSection{
  position:relative;
  width:100%;
  padding:2rem var(--container-padding);
  background:transparent;
  overflow:hidden;
}

.tslnLegalSectionPage{
  position:relative;
  z-index:2;
  width:min(100%, 72rem);
  margin-inline:auto;
}

.tslnLegalSection{
  margin-bottom:2rem;
}

.tslnLegalSectionContentheader{
  position:relative;
  min-height:5rem;
  overflow:visible;
  isolation:isolate;
}


/* T MARK visible dans le top de la page */
.tslnLegalSectionContentheader .uiHeroMarkDecor{
  position:fixed;
  top:230;
  right:100;
  z-index:-1;
  pointer-events:none;
  opacity:1;
}

.tslnLegalSectionContentheader .uiHeroMarkDecor span{
  display:block;
  font-size:40rem;
  line-height:1;
  font-style:italic;
  font-weight:800;
  color:transparent !important;
  -webkit-text-fill-color:var(--tsln-muted);
  -webkit-text-stroke:2px var(--tsln-muted);
  opacity:.15;
}




.tslnLegalSectionContent h2{
  margin:0 0 1rem;
  color:var(--tsln-blue-dark);
  font-size:clamp(1.2rem, 4vw, 1.45rem);
  font-weight:var(--font-weight-h1);
  line-height:1.1;
  margin-top: 2rem;
  text-align: center;
}

.tslnLegalSectionTitle {
    font-size: var(--text-lg);
    font-weight: var(--font-weight-title);
}

.tslnLegalSectionContent p{
  margin:0;
  color:var(--tsln-blue-dark);
  font-weight: var(--font-weight-medium);
  font-size:var(--text-xs);
  margin-bottom: 1rem;
}



/* =========================================
   TABLE OF CONTENTS
========================================= */

.static-toc-wrap{
  margin-bottom:2rem;
}

.termsDate{
  display:flex;
  align-items:center;
  gap:.5rem;
   flex-wrap:nowrap;
   border-bottom: 2px solid var(--tsln-blue);
  padding-bottom: 1rem;
  
}

.termsDate strong,
.termsDate p{
  margin:0;
  font-size: var(--text-small);
  text-align: center;
}

.termsDate strong {
    font-weight: var(--font-weight-medium);
}

.termsDate p{
    font-weight: var(--font-weight-medium);
    font-style: italic;
}

.sommaryterms h2{
  margin:0 0 1rem;
  color:var(--tsln-blue-dark);
  font-size:var(--text-lg);
  text-transform: uppercase;
  font-weight:var(--font-weight-title);
}


.LegalScrollLink{
  display:grid;
  grid-template-columns:repeat(2,minmax(0,1fr));
  gap:0 2rem;
  margin:0;
  padding-left:1.25rem;
  padding: 2rem 0;
}

.tslnLegalSectionContent li{
  margin:0;
}

.tslnLegalSectionContent a{
  color:var(--tsln-blue);
  font-size: var(--text-xs);
  font-weight: var(--font-weight-medium);
  text-decoration:none;
  transition:var(--transition-fast);
}

.tslnLegalSectionContent a:hover {
  color:var(--tsln-muted);
}


.tslnLegalSectionContent{
  margin-top:3rem;
  scroll-margin-top:8rem;
}


.tslnLegalSectionContent h3{
  margin:1.5rem 0 .75rem;
  color:var(--tsln-blue-dark);
  font-size:var(--text-md);
  font-weight:var(--font-weight-title);
}


.tslnLegalSectionContent ul,
.tslnLegalSectionContent ol{
  margin:0 0 1rem;
  padding-left:1.5rem;
  font-size: var(--text-xs);
  color: var(--tsln-muted);
  margin: 1rem 0;
}

.tslnLegalSectionContent li{
  margin-bottom:.2rem;
  font-size: var(--text-xs);
  color:var(--tsln-text);
  line-height:var(--lh-normal);
}

.tslnLegalSectionContent strong{
  color:var(--tsln-blue-dark);
  font-weight: var(--font-weight-medium);
}



/* =========================================
   MOBILE
========================================= */

@media(max-width:768px){

    .tslnLegalSection{
    width:100%;
}

    .tslnLegalSection{
    padding:0rem 1rem;
  }
  

  .LegalScrollLink{
    grid-template-columns:1fr;
  }

}


/* ===== FILE: TrustGrid.css ===== */

/* =========================================
   TRUST GRID
========================================= */

.tslnTrustGridSection {
  position: relative;
  z-index: 1;
  width: 100%;
  padding: 3rem var(--container-padding);
  background: var(--tsln-white);
  margin-bottom: 0;
}

.tslnTrustGrid{
  width:min(100%, var(--container-max));
  margin-inline:auto;
  display:flex;
  align-items:stretch;
  gap:1rem;
}

.tslnTrustCard{
  flex:1;
  min-height:12rem;
  padding:1.5rem;
  border-radius:0;
  background:var(--tsln-white);
}

.tslnTrustCard h3{
  margin:0 0 .75rem;
  color:var(--tsln-blue-dark);
  font-size:var(--text-xl);
  font-weight:var(--font-weight-h1);
  line-height:normal;
}

.tslnTrustCard p{
  margin:0;
  color:var(--tsln-blue-dark);
  font-weight: var(--font-weight-medium);
  line-height:var(--lh-normal);
}

@media(max-width:768px){

  .tslnTrustGridSection{
    padding:2rem 0rem;
  }

  .tslnTrustGrid{
    flex-direction:column;
  }

  .tslnTrustCard{
    min-height:8rem;
  border-bottom: 0;
  }

  .tslnTrustCard h3{
  font-size:1.5rem;
}
}

/* ===== FILE: UiSkeleton.css ===== */

/* =========================================
   UI SKELETON UNIVERSAL
========================================= */

.uiSkeletonPage{
  width:100%;
  padding:2rem var(--container-padding);
}

.uiSkeletonHeader,
.uiSkeletonSection,
.uiSkeletonFooter,
.uiSkeletonAccordion span,
.uiSkeletonLine{
  position:relative;
  overflow:hidden;
  background:var(--tsln-bg-muted);
}

.uiSkeletonHeader,
.uiSkeletonSection,
.uiSkeletonFooter{
  width:min(100%, var(--container-max));
  margin-inline:auto;
  border-radius:var(--radius-sm);
}

.uiSkeletonHeader{
  height:4rem;
  display:flex;
  align-items:center;
  gap:1rem;
  padding:1rem;
}

.uiSkeletonHeader span{
  height:1rem;
  border-radius:var(--radius-full);
  background:var(--tsln-border);
}

.uiSkeletonHeader span:nth-child(1){ width:10rem; margin-right:auto; }
.uiSkeletonHeader span:nth-child(2){ width:6rem; }
.uiSkeletonHeader span:nth-child(3){ width:6rem; }

.uiSkeletonSection{
  min-height:16rem;
  margin-top:2rem;
  padding:2rem;
}

.uiSkeletonLines{
  display:grid;
  gap:.75rem;
}

.uiSkeletonLine{
  display:block;
  height:1rem;
  border-radius:var(--radius-full);
}

.uiSkeletonLine.title{
  width:min(100%, 28rem);
  height:2rem;
}

.uiSkeletonLine.text:nth-child(1){ width:100%; }
.uiSkeletonLine.text:nth-child(2){ width:82%; }
.uiSkeletonLine.text:nth-child(3){ width:64%; }

.uiSkeletonLine.button{
  width:10rem;
  height:2.75rem;
  margin-top:1rem;
  border-radius:var(--radius-full);
}

.uiSkeletonAccordion{
  width:min(100%, var(--container-max));
  margin:2rem auto 0;
  display:grid;
  gap:.85rem;
}

.uiSkeletonAccordion span{
  height:4rem;
  border-radius:var(--radius-sm);
}

.uiSkeletonFooter{
  height:10rem;
  margin-top:2rem;
}

.uiSkeletonHeader::after,
.uiSkeletonSection::after,
.uiSkeletonFooter::after,
.uiSkeletonAccordion span::after,
.uiSkeletonLine::after{
  content:"";
  position:absolute;
  inset:0;
  transform:translateX(-100%);
  background:linear-gradient(
    90deg,
    transparent,
    rgba(255,255,255,.45),
    transparent
  );
  animation:uiSkeletonWave 1.4s ease-in-out infinite;
}

@keyframes uiSkeletonWave{
  100%{
    transform:translateX(100%);
  }
}


/* =========================================
   NETWORK STATUS OVERLAY
========================================= */

.tslnNetworkOverlay{
  position:fixed;
  inset:0;
  z-index:99997;
  display:flex;
  align-items:center;
  justify-content:center;
  padding:1rem;
  background:rgba(255,255,255,.82);
  backdrop-filter:blur(10px);
}

.tslnNetworkCard{
  width:min(100%, 34rem);
  padding:1.5rem;
  border-radius:var(--radius-md);
  background:var(--tsln-white);
  box-shadow:var(--shadow-md);
  border:1px solid var(--tsln-border);
}

.tslnNetworkCard h2{
  margin:0 0 .75rem;
  color:var(--tsln-blue-dark);
}

.tslnNetworkCard p{
  margin:0 0 1rem;
  color:var(--tsln-muted);
  line-height:1.7;
}


.uiSkeletonLine,
.uiSkeletonHeader span,
.uiSkeletonAccordion span,
.uiSkeletonFooter span,
.uiSkeletonCard{
  background:linear-gradient(
    90deg,
    #f1f5f9 0%,
    #e5e7eb 50%,
    #f1f5f9 100%
  );
  background-size:200% 100%;
  animation:uiSkeletonPulse 1.4s ease-in-out infinite;
  border-radius:var(--radius-sm);
}

@keyframes uiSkeletonPulse{
  0%{ background-position:200% 0; }
  100%{ background-position:-200% 0; }
}


@media(max-width:768px){
  .uiSkeletonPage{
    padding:1rem;
  }

  .uiSkeletonSection{
    padding:1rem;
  }

  .uiSkeletonHeader span:nth-child(2),
  .uiSkeletonHeader span:nth-child(3){
    display:none;
  }
}



/* =========================================
   APPLE STYLE SKELETON
========================================= */

.tslnSkeletonLine,
.tslnSkeletonPrice{
  display:block;
  border-radius:var(--radius-sm);
  background:linear-gradient(
    90deg,
    var(--tsln-bg-muted) 0%,
    #e5e7eb 45%,
    var(--tsln-bg-muted) 100%
  );
  background-size:200% 100%;
  animation:tslnSkeletonMove 1.25s ease-in-out infinite;
}

.tslnSkeletonLine{
  height:.85rem;
  margin-bottom:.65rem;
}

.tslnSkeletonLine.wide{
  width:12rem;
}

.tslnSkeletonLine.medium{
  width:8rem;
}

.tslnSkeletonLine.small{
  width:5rem;
}

.tslnSkeletonPrice{
  width:5rem;
  height:2.25rem;
}

.tslnTicketCardSkeleton{
  pointer-events:none;
}

@keyframes tslnSkeletonMove{
  0%{
    background-position:200% 0;
  }

  100%{
    background-position:-200% 0;
  }
}

/* ===== FILE: UiTitle.css ===== */

/* =========================================
   UI TITLE
========================================= */

.uiTitle{
  position:relative;
  display:inline-block;
  margin:0 0 1.25rem;
  color:var(--tsln-blue-dark);
  font-size:1.35rem !important;
  font-weight:var(--font-weight-h1);
  line-height:var(--lh-tight);
  letter-spacing:var(--letter-normal);
}

.uiTitle::after{
  content:"";
  position:absolute;
  left:0;
  display: none;
  padding: .10rem;
  bottom:-.5rem;
  width:4rem;
  height:.25rem;
  border-radius:var(--radius-xs);
  background:var(--tsln-blue);
}

.uiTitleCenter{
  display:block;
  text-align:center;
}

.uiTitleCenter::after{
  left:50%;
  transform:translateX(-50%);
}

.uiTitleLight{
  color:var(--tsln-white);
}

.uiTitleLight::after{
  background:var(--tsln-white);
}

/* MOBILE */

@media(max-width:768px){

  .uiTitle{
    font-size:var(--text-lg);
  }

  .uiTitle::after{
    width:3rem;
    height:.2rem;
  }

}
